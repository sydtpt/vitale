---
topic: A Retrospectiva vira revista — cadernos por período, bases nomeadas, sol como camada e a lua sob pré-registro (épico 1: só dado já gravado)
updated: 2026-09-08T16:07
---

- (event) create a partir de _bmad-output/planning-artifacts/revista-retrospectiva-corte-v1.md (mesa de party mode, 07/09/2026) + brief. Input rico: distilacao direta, sem elicitacao.
- (decision) DOIS EPICOS. Epico 1 (este spec) = a revista sobre dado JA GRAVADO, mais sol e lua, retroativo a 22/05/2023. Epico 2 (fora) = captura nova, que so rende analise a partir de mai-jun/2027. Razao: os dois nao compartilham cronograma nem risco, e juntar produz um epico que nao termina.
- (capability) CAP-1 edicao composta por cadernos: chave (user_id, tipo_periodo, inicio, fim, caderno), cada caderno com texto, assinatura e posicao propria; errata por caderno.
- (capability) CAP-2 quatro cadernos (Sono, Movimento, Coracao, Rotina) mais a capa. Ver cadernos.md.
- (capability) CAP-3 um pacote de fatos POR CADERNO, escopo restrito ao caderno.
- (capability) CAP-4 tres bases nomeadas (B1 periodo anterior, B2 mesmo periodo do ano anterior, B3 a normal dele para este periodo) mais TRAJETORIA como fato derivado.
- (capability) CAP-5 o texto e obrigado a NOMEAR a base; base citada sem nome REPROVA na verificacao (quinta regra do verificar.ts).
- (capability) CAP-6 camada de luz do dia em todos os pacotes, derivada na leitura e nunca gravada.
- (capability) CAP-7 ordem do miolo ranqueada por notabilidade (afastamento x confianca, com portao de n), congelada na impressao.
- (capability) CAP-8 sumario de quatro chamadas, tocavel, reaproveitando a manchete que cada caderno ja escreveu.
- (capability) CAP-9 tres formas por tipo de periodo: postal (semana), edicao (mes e trimestre), anuario (ano).
- (capability) CAP-10 capa: foto quando existe, tracado do proprio periodo quando nao; e o arquivo como parede de capas.
- (capability) CAP-11 ausencia declarada: caderno vazio some, caderno cego vira lapide, base inexistente entra no pacote como fato.
- (capability) CAP-12 pagina da lua sob pre-registro imutavel, com moldura fixa, tres vereditos e contador de execucoes.
- (capability) CAP-13 impressao em massa fora do aparelho, por script, com o mesmo nucleo puro.
- (capability) CAP-14 o leitor pode SILENCIAR um caderno (hidden). Nao pode reordenar (order aposentado).
- (constraint) As leis herdadas do brief §2, todas mantidas: e jornal (informa, nao aconselha); estatistica acha e LLM prioriza e narra (o modelo nunca calcula, nunca deriva numero que nao recebeu, nunca afirma causa); todo numero citado existe no pacote, por verificacao mecanica; cobertura desigual obriga ressalva; periodo fechado congela e vira errata, nunca reescrita, e all nao tem edicao; provedor e modelo sao configuracao (ADR 0040), nucleo puro sem SDK nem rede; a camada narra e nunca decide; mobile-first.
- (constraint) A revista NUNCA gera sozinha: abrir a retro so LE. Imprimir custa dinheiro e e ato do usuario — senao folhear seis meses dispararia seis chamadas pagas.
- (constraint) O SOL E PRE-REQUISITO DA LUA. Sem horas de luz como covariavel, um achado lunar e um achado sazonal com outro nome: as 290 noites cobrem 502 dias (58%), com o verao de 2025 parcialmente ausente, e a janela lunar anda pelo calendario.
- (constraint) A ordem e uma COLUNA (posicao smallint + unique por periodo), nao um array e nao uma tabela cabecalho: com a chave por caderno cada caderno e uma linha, e um array com a ordem do conjunto guardado em cada parte diverge.
- (constraint) A ordem congela na impressao. A ordem sai do dado, mas a FUNCAO e codigo: ajustar o peso da confianca depois faria um periodo fechado se reordenar sozinho, que e reescrita silenciosa.
- (constraint) O pre-registro da lua e IMUTAVEL. Sua sha256 e constante no codigo; hash divergente com execucao ja gravada QUEBRA O BUILD. Reexecucao a cada +100 noites, com contador visivel na pagina.
- (constraint) season continua trimestre civil. Estacao nao e periodo, e contexto — mover fronteira quebraria a edicao de season ja gravada em producao.
- (constraint) Backfill: verifica ANTES de gravar; assinatura identica (provedor, modelo, prompt_versao, PACOTE_VERSAO, agg_version_no_momento); e PAGINACAO obrigatoria, porque o PostgREST corta em 1000 linhas sem erro e health_daily tem 4.385.
- (constraint) Mais numeros autorizados enfraquecem a verificacao. Medido em 07/09 no pacote real de agosto: 71 valores exatos, dos quais 16 sao inteiros entre 0 e 100 — um inteiro alucinado nessa faixa passa 16% das vezes HOJE, com uma base so. E o alfabeto de agosto e com correlacoes, eventos e lacunas VAZIOS.
- (decision) A capa NAO ganha coluna: a manchete e a do caderno em posicao=1 e a foto sai de coverOf, que ja e funcao pura, com a correcao manual ja gravada em activity_photos. A edicao inteira sao quatro linhas.
- (decision) As 7 edicoes hoje em producao (prompt_versao 2, pacote_versao 1) NAO viram errata — nada que dizem ficou falso; ficaram antigas. Texto exportado para docs/specs/revista-retrospectiva/primeiras-edicoes-prompt-v2.md (da diff contra o prompt v3, que a tabela nao daria) e as linhas saem NA MIGRATION, nao antes: hoje o caminho de leitura ainda as toca e uma delas tem horas de vida.
- (decision) Tres coisas que pareciam cadernos nao sao, e a regra vale: material que ATRAVESSA cadernos nao e caderno. Geral virou capa; Sol virou camada; Onde virou capa + fatos de Movimento. Corpo virou Coracao (das 14 metricas, 4 sao cadaveres, 8 nunca chegaram, peso tem 1 medida).
- (direction) O dono derrubou a recomendacao da mesa (miolo fixo pelo retro_prefs, capa variavel) e escolheu RANQUEAR O MIOLO, aposentando RetroPrefs.order. Mantido o hidden. Sem migration: resolveRetroPrefs e defensivo, entao a chave order passa a ser ignorada e o jsonb em producao fica como esta.
- (direction) O dono mandou MANTER o caderno Rotina contra o corte do PM. Argumento vencedor: os outros tres cadernos contam o que aconteceu com o corpo; nenhum conta o que ele decidiu. Rotina entra so com trajetoria, sem base anual ate mai/2027.
- (assumption) Assumido que o backfill em massa cobre mes, trimestre e ano (436 edicoes, ~US$1,20 a US$4,80 ao preco medido de US$0,011/chamada) e que semana e impressa sob demanda, ja que o postal nao grava edicao.
- (question) A coordenada do usuario para a efemeride passa a ser configuracao — onde ela mora e o que acontece com quem viaja? Rotas tem lat/long; dias sem atividade nao tem nenhuma.
- (question) ADR 0045 (resultado negativo publica com o mesmo destaque do positivo) ainda nao foi escrita, e a numeracao de ADR ja colidiu duas vezes entre branches neste repositorio.
- (question) As quatro metricas mortas (respiracao 10/07, VO2max 14/07, SpO2 16/07, aneis 17/08) precisam de investigacao INDEPENDENTE: cano quebrado ou habito que mudou? A lapide narra o fato, mas nao diagnostica a causa.
- (event) Self-validate passe 1 (coerencia, regras 1-6 e 8): PASSOU. 14 capacidades, todas com intent e success testaveis; IDs CAP-1..14 unicos. Uma correcao aplicada: o intent de CAP-13 prescrevia implementacao ("impresso por script") e foi reescrito para o QUE ("o arquivo historico pode ser impresso em massa sem o aparelho"), com o script descendo para o success e para mudancas-mecanicas.md. Nao-objetivos explicitos: 11. Sinal de sucesso concreto e demonstravel.
- (event) Self-validate passe 2 (preservacao, regra 7): PASSOU. Todas as secoes do corte-v1 aterrissaram: §0 dois epicos -> Why e nao-objetivos; §1 cadernos -> CAP-2 e cadernos.md; §2 bases e a medicao do alfabeto -> CAP-4/CAP-5, restricao e bases-e-ranqueamento.md; §2b forma -> CAP-7/8/9/10 e cadernos.md; §3 captura -> nao-objetivos; §4 destino do balde -> success de CAP-2 e cadernos.md; §5 sol e lua -> CAP-6/CAP-12 e restricoes; §6 corte -> nao-objetivos; §7 backfill -> CAP-13 e mudancas-mecanicas.md; §8 ausencia -> CAP-11; §9 mecanicas -> mudancas-mecanicas.md; §10 aberto -> open questions. CONTEUDO SO-DE-CERIMONIA DESCARTADO, registrado para nao ser silencioso: a atribuicao nominal dos argumentos na mesa (quem defendeu o que, quem cedeu, a recomendacao que o dono derrubou e por que) — e narrativa de processo, preservada no corte-v1 e no memlog do party mode, e nenhum consumidor a jusante muda decisao sem ela.
- (event) spec.md derivado: 14 capacidades, 4 companions (cadernos.md, bases-e-ranqueamento.md, mudancas-mecanicas.md, pre-registro-lua.md), 2 sources, 3 assumptions, 3 open questions.
- (direction) Coordenada da efemeride: o dono decidiu "tratar como normal" — uma coordenada de CASA, unica e fixa (~50,8 N, Belgica), em configuracao. Viagem NAO e modelada: periodo passado fora do pais recebe a luz do dia da casa. Razao: nao ha modelo de lugar ainda; quem traz lat/long por dia e o epico 2 (presenca), e so entao isso pode mudar. Vira assuncao declarada, nao pergunta aberta.
- (decision) ADR 0045 ESCRITA — "o resultado negativo publica com o mesmo destaque do positivo". Numeracao 0045 conferida contra main e todas as branches locais e remotas: livre. Estende a lei do brief: informar inclui informar que nao ha o que informar. Quatro consequencias operacionais: moldura fixa e veredito variavel; TRES vereditos (achado / nenhum padrao / inconclusivo com noites faltantes); sem atenuacao visual; contador de execucoes visivel.
- (decision) AS QUATRO METRICAS MORTAS: DIAGNOSTICADAS POR MEDICAO EM PRODUCAO (07/09) — e a resposta e TROCA DE RELOGIO, nao cano quebrado. Provas: (1) a fonte da VFC no extra jsonb migra de Apple para "intervals" em julho/2026 (10 dias de cada em julho, 27 de intervals em agosto); (2) as horas de STAND dos aneis, que exigem Apple Watch no pulso, caem de 11,6 (maio) para 9,0 (junho), 5,8 (julho) e 5,0 (agosto), e os dias com anel caem de 26-28 para 14 antes de parar em 17/08. As quatro mortas — respiracao 10/07, vo2max 14/07, spo2 16/07, aneis 17/08 — sao exatamente as EXCLUSIVAS do Apple Watch; as sobreviventes (fcRepouso, sono, vfc, passos, andares, energia) chegam pelo Garmin via intervals.icu. CONSEQUENCIA PARA O SPEC: nao ha bug a consertar antes de construir, e a LAPIDE (CAP-11) segue necessaria — a revista continua tendo que dizer que ficou cega, so que a causa e biografica e nao tecnica. Tambem descoberto de passagem: PESO tem 1 medida (16/07/2026) e nunca mais.
- (event) Re-derivacao 07/09: as tres perguntas abertas foram resolvidas; spec.md agora tem 0 open questions bloqueantes e 4 assumptions.
- (event) UPDATE 08/09/2026 disparado pela passagem de bmad-ux (_bmad-output/planning-artifacts/ux-designs/ux-revista-retrospectiva-2026-09-07/). Tres revisores acharam SETE defeitos DENTRO deste contrato, que declarava zero perguntas abertas, mais dois bloqueadores nao declarados. Oito resolvidos pelo dono; um deixado aberto de proposito.
- (decision) A LUA GANHA TABELA PROPRIA (lua_execucoes): uma linha por execucao, com hash do pre-registro, veredito, contagens e data. Razao: em edicoes_ia ela nao cabe - o CHECK recusa caderno='lua', a chave primaria impede o 'acumula, nunca substitui' do §7.2, e o teste roda sobre TODO o historico, que nao e week/month/season/year (e all e recusado). Acumular vira natural e edicoes_ia continua sendo so edicao de periodo.
- (constraint) CONSEQUENCIA da tabela propria: isto CONTRARIA o §7.2 do pre-registro, que manda gravar em edicoes_ia e e IMUTAVEL. O proprio documento prescreve a saida - 'correcao so por documento novo, que cita este e diz o que mudou e por que'. Esse documento de correcao e ARTEFATO OBRIGATORIO antes de construir a lua, e o pre-registro-lua.md NAO e editado.
- (decision) A CAPA GANHA COLUNA. SUPERSEDE a decisao de 07/09 ('a capa NAO ganha coluna'). Razao: coverOf le isCover e state==='linked', os dois mutaveis depois da impressao (a estrela, o vinculo automatico de 40 m, o ph:// que some da biblioteca) - a foto de agosto virava outra seis semanas depois, contra 'periodo fechado congela' e contra o proprio sinal de sucesso do spec. A foto escolhida passa a ser carimbada na impressao. A edicao deixa de ser 'quatro linhas e nada mais'.
- (decision) LAPIDE VENCE CADERNO VAZIO. Um caderno cujo unico conteudo e a lapide EXISTE, lidera, e diz que ficou cego. Razao: o mes em que o relogio para e justamente o mes sem dado, e o passo 6 apagaria a lapide exatamente quando ela importa - a revista ficaria cega sobre a propria cegueira, que e o que CAP-11 existe para impedir. Os passos 5 e 6 de ordenarCadernos ganham precedencia explicita.
- (decision) A CHAMADA E A PRIMEIRA FRASE DO TEXTO DO CADERNO, cortada no primeiro ponto final. Extracao mecanica: custo zero de geracao, nada novo a verificar pelas cinco regras, e a manchete e literalmente o que o caderno diz primeiro. Consequencia: o prompt passa a dizer ao modelo que a primeira frase vira capa e sumario. Isto DESBLOQUEIA CAP-8 e CAP-10, que nao eram construiveis.
- (decision) REIMPRESSAO PARCIAL RECALCULA E REGRAVA A ORDEM INTEIRA, em transacao. Nenhuma linha orfa, nenhuma posicao reservada sem texto, nenhum choque com unique (user_id, tipo_periodo, inicio, fim, posicao).
- (constraint) AMENDA a restricao 'a ordem congela na impressao': ela congela na ULTIMA impressao. Reabrir nao reordena - so um ato explicito de reimprimir reordena, e ele reordena o conjunto todo. O success de CAP-7 sobrevive intacto, porque reabrir seis semanas depois nao e reimprimir.
- (decision) ERRATA EM MASSA FICA: um bump de AGG_VERSION marca todas as edicoes, e isso esta certo - se a agregacao mudou, toda edicao anterior e potencialmente velha, e dizer isso e honesto. Custo aceito e declarado: uma errata em ~1.744 edicoes de uma vez e indistinguivel de ruido.
- (decision) A TIRA DO ANUARIO MEDE O FATO QUE LIDEROU O RANQUEAMENTO daquele caderno, mes a mes. A tira e o ranqueamento passam a falar do mesmo numero. Custo declarado: se a metrica lider muda de mes para mes, a tira mistura unidades. Isto desbloqueia a abertura serial de CAP-9.
- (question) CAP-14 sem superficie: hidden e Partial<Record<RetroBlockId,string>> e RetroBlockId nao contem caderno nenhum (retro-blocks.ts:87), mas o contrato diz 'sem migration'. Alargar o tipo ou criar chave separada? O dono deixou ABERTO de proposito: e decisao de arquitetura, e 'sem migration' precisa ser reconferido por quem a tomar.
- (event) Self-validate passe 1 (coerencia, regras 1-6 e 8): PASSOU. 14 capacidades, todas com intent e success; IDs CAP-1..14 estaveis, nenhum renumerado nem reusado. 11 nao-objetivos. 19 restricoes (eram 17; +2: o documento de correcao do pre-registro e a errata em massa do AGG_VERSION). 1 pergunta aberta, bloqueante e deliberada (CAP-14). Uma correcao aplicada: o intent de CAP-7 dizia 'a edicao impressa nunca se reordena depois', absoluto demais depois da decisao da reimpressao parcial - reescrito para 'nao se reordena por conta propria; so um ato explicito de reimprimir a reordena', com o success preservando a promessa de reabrir.
- (event) Self-validate passe 2 (preservacao, regra 7): PASSOU. Os sete defeitos e os dois bloqueadores da passagem de UX aterrissaram: lua sem chave -> CAP-12 + mudancas-mecanicas §lua_execucoes + restricao do documento de correcao; capa nao congela -> CAP-10 + cadernos §A capa + mudancas-mecanicas; lapide x vazio -> CAP-11 + bases-e-ranqueamento §passo 5 vence o 6; posicao na reimpressao -> CAP-7 + restricao da ordem + mudancas-mecanicas; errata em massa -> mudancas-mecanicas §AGG_VERSION; ADR 0045 -> assuncao; origem da chamada -> CAP-8 + cadernos §O sumario; o que a tira mede -> CAP-9 + cadernos §Anuario; hidden -> open question. As duas espinhas de UX viraram companions ADOTADOS (bmad-spec nao as edita). pre-registro-lua.md NAO foi tocado - confirmado por git status.
- (event) spec.md re-derivado 08/09: 14 capacidades, 6 companions (4 proprios + DESIGN.md e EXPERIENCE.md adotados), 2 sources, 5 assumptions, 1 open question.
- (event) UPDATE 08/09/2026 disparado pela passagem de arquitetura (_bmad-output/planning-artifacts/architecture/architecture-Orbe-revista-2026-09-08/, 18 ADs). Ela NAO redecidiu nada do contrato: resolveu os sete itens que o contrato deixara para a arquitetura, e trouxe quatro contradicoes novas. Reconciliacao cirurgica — as 14 capacidades e as 19 restricoes nao foram reescritas.
- (decision) CAP-14 RESOLVIDA — a unica pergunta aberta do spec fecha. hidden de caderno mora em CHAVE PROPRIA no mesmo jsonb: RetroPrefs.cadernosOcultos: Partial<Record<CadernoId,string>>, com CadernoId nascendo como vocabulario de dono unico no nucleo. RetroBlockId NAO e alargado — caderno herdaria order/kinds/fixed, e 'order' e exatamente a ordem do leitor que CAP-7 aposentou; um campo que precisa ser ignorado e o caminho por onde alguem a reintroduz por visibleBlocks. 'Sem migration' RECONFERIDO e continua verdade: user_preferences.retro_prefs e jsonb not null default {} SEM CHECK de forma, e resolveRetroPrefs ja descarta chave desconhecida em silencio. Decidido pelo dono em 08/09 sobre quatro opcoes; descartadas mapear caderno->bloco existente (nao e injetivo: Movimento=fitness+sports, Rotina=tasks+dailyTasks+habits, e funde esconder o card de numeros com silenciar o caderno narrado) e coluna nova (unica que quebra 'sem migration'). Ver AD-2 da espinha. O spec passa a ter ZERO perguntas abertas.
- (decision) A ASSUNCAO DA ADR 0045 VIRA PRE-REQUISITO CUMPRIDO. A emenda foi escrita: docs/decisions/0046-a-linha-de-entrada-carrega-o-veredito.md, que SUPERSEDE SO A CLAUSULA 3 da 0045 — as clausulas 1, 2 e 4 seguem intactas. Argumento: das cinco formas de atenuacao que a clausula 3 lista, quatro atenuam sozinhas e 'atras de um toque' atenua POR PRIVACAO, e a linha de entrada que carrega o veredito por extenso elimina a privacao. Tres exigencias substantivas, e a falha de qualquer uma restaura a proibicao original. Numeracao 0046 conferida contra main e todas as branches locais e remotas — a propria 0045 pedia essa conferencia por escrito.
- (decision) correcao-pre-registro-lua.md ENTRA EM companions: — ele e pre-requisito de construcao da lua, exigido pela propria restricao do spec, e nao estava listado em lugar nenhum do contrato. Corrige DUAS clausulas do §7: a 7.2 (a execucao grava em lua_execucoes, nao em edicoes_ia — tres impedimentos independentes) e a 7.3 (a barreira do hash e incondicional, porque a condicional 'com execucao ja gravada' nao e construivel: architecture.test.ts roda offline com npx tsx e 'execucao gravada' mora no Postgres). Declara §1-§6 e §8 intocados, e registra a sha256 do arquivo corrigido. O pre-registro-lua.md NAO foi editado — e o unico companion desta pasta que bmad-spec nao re-deriva, porque ele se declara imutavel e vale por isso.
- (decision) A ESPINHA DE ARQUITETURA ENTRA EM companions: (_bmad-output/planning-artifacts/architecture/architecture-Orbe-revista-2026-09-08/ARCHITECTURE-SPINE.md), como companion ADOTADO — bmad-spec nao a edita. Razao: as stories precisam citar os AD por id, e sete decisoes que o contrato deixou explicitamente para a arquitetura so existem la.
- (change) cadernos.md §A capa CORRIGIDO: a frase 'nao ganha coluna no banco' era a posicao de 07/09, revogada em 08/09 pela decisao de carimbar, e sobrevivia contradizendo o proprio paragrafo doze linhas abaixo. O companion parou de nomear armazenamento — ONDE o carimbo mora e decisao de arquitetura — e ganhou o que faltava: o carimbo congela a ESCOLHA e a LEGENDA, nunca a imagem. Foto apagada da biblioteca do iPhone some com carimbo ou sem, porque o app nunca teve o arquivo; e por isso que a legenda e gravada como valor e nao re-derivada.
- (change) DESIGN.md §Capa corrigido com a mesma frase revogada, e com nota do que dizia antes e por que mudou. E companion ADOTADO — bmad-spec nao o edita —, entao registre-se que a edicao foi feita pela passagem de arquitetura com autorizacao explicita do dono, nao por este skill. Se bmad-ux rodar sobre esta frente, conferir que ela reconhece a edicao em vez de reescrever por cima.
- (decision) OS SETE ITENS que o contrato deixara para a arquitetura, agora fechados e citaveis por AD: AD-1 a revista mora em rota propria /revista/[tipo]/[inicio] com o EdicaoCard como porta, e a Retrospectiva fica intacta (os 13 blocos nao podem morrer — 'all' e periodo em curso nunca tem edicao, entao os blocos sao o conteudo inteiro deles); AD-2 CAP-14; AD-3 a capa carimbada em edicoes_capa, tabela a parte, porque com a PK por caderno uma coluna seriam quatro copias; AD-4 a ordem recalculada por funcao no banco com a unique da posicao deferrable initially deferred; AD-5/AD-6/AD-7 lua_execucoes, o teste em sleep/lua.ts e a barreira incondicional; AD-8 onAccent; AD-9 sendAccessibilityEvent. Mais seis ADs que o portao de revisao arrancou, das quais a mais grave e AD-16.
- (question) CONTRADICAO NOVA (i), trazida e nao resolvida: mudancas-mecanicas.md lista a efemeride como 'novo, funcao pura de data e coordenada', mas packages/shared/src/astro/ JA EXISTE com sun.ts (Meeus com termos seculares, 36 s de erro contra o USNO), moon.ts e timezone-coords.ts. O que falta e a agregacao de horas de luz por periodo e o instante das fases. O companion segue com a palavra errada.
- (question) CONTRADICAO NOVA (ii): a frase do §7.3 do pre-registro ('hash divergente COM EXECUCAO JA GRAVADA quebra o build') nao era construivel, e segue de pe no arquivo imutavel. Resolvida pela correcao, que e como um pre-registro se corrige — mas quem ler so o §7.3 le uma regra que nao existe.
- (question) CONTRADICAO NOVA (iii): EXPERIENCE.md §Accessibility Floor e o validation-report nomeiam setAccessibilityFocus, que a doc atual do React Native marca como DEPRECADO em favor de sendAccessibilityEvent(ref,'focus') — com ref, sem findNodeHandle. O RN 0.86.3 instalado expoe as duas. AD-9 manda a atual; os dois documentos de UX seguem nomeando a antiga.
- (question) CONTRADICAO NOVA (iv), a mais afiada e achada pelo revisor de verificacao contra a propria espinha: a janela lunar. O §3 pre-registrou 'as 5 noites que ANTECEDEM a cheia (fase -5 a -1)', que EXCLUI a noite da cheia. A espinha havia escrito (cheia-5d, cheia], fechada a direita, o que incluiria a noite da cheia e excluiria a -5 — a coluna testada anda uma noite, e a que entra e a de maior valor esperado sob a hipotese. Corrigida na espinha para [cheia-5d, cheia). O contrato nao precisa mudar; fica registrado porque e o tipo de desvio que se disfarca de melhoria de precisao.
- (event) Self-validate passe 1 (coerencia, regras 1-6 e 8): PASSOU. 14 capacidades, todas com intent e success; IDs CAP-1..14 estaveis, nenhum renumerado nem reusado. 11 nao-objetivos. Restricoes: 19, mesmo numero — duas foram REESCRITAS, nao acrescentadas (a do documento de correcao, que virou fato cumprido, e a do AGG_VERSION, que ganhou a ressalva de que HOJE ela nao vale). ZERO perguntas abertas. Uma correcao aplicada no passe: o success de CAP-14 dizia 'no mesmo jsonb de preferencias', que prescreve armazenamento — reescrito para 'nas preferencias do leitor sem migration', preservando a promessa testavel sem nomear a forma.
- (event) Self-validate passe 2 (preservacao, regra 7): PASSOU. As quatro reconciliacoes aterrissaram (CAP-14 -> success + Open Questions; ADR 0046 -> assuncao virou pre-requisito cumprido; correcao-pre-registro-lua.md e a espinha -> companions:). As quatro contradicoes novas aterrissaram como 'Correcoes de texto pendentes', fora do bloco de perguntas abertas porque a resposta certa ja e conhecida — sao cleanup, nao decisao. ACHADO DE PRESERVACAO no passe: a restricao do AGG_VERSION afirmava um mecanismo que NAO FUNCIONA em producao hoje; o spec passou a dizer isso e a ordenar o conserto antes do backfill. Os 18 ADs NAO foram absorvidos no kernel de proposito — vivem na espinha, que agora e companion, e e la que as stories os citam por id.
