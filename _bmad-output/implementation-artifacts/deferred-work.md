- source_spec: `_bmad-output/implementation-artifacts/spec-testes-do-web-com-vitest.md`
  summary: `packages/shared` tem 3 arquivos de teste que nenhum runner executa — `src/goals/evaluate.test.ts`, `src/chart/axis.test.ts`, `src/chart/smooth-path.test.ts`.
  evidence: O script `test` do pacote é `echo 'No tests yet'`; o jest do mobile tem `rootDir` em `mobile/` e `npx jest --listTests | grep -c packages/shared` devolve 0; o novo `web/tsconfig.spec.json` cobre só `web/src`. Mesma classe de problema que esta story resolveu no web — testes escritos que nunca rodam.

- source_spec: `_bmad-output/implementation-artifacts/spec-testes-do-web-com-vitest.md`
  summary: `mobile/src/lib/planned-match.ts` não tem nenhum teste, embora seja espelho de código que o web testa com 9 casos.
  evidence: `find mobile -name '*planned-match*'` devolve só o fonte; o jest do mobile roda 29 suítes, exatamente os 29 arquivos de `mobile/src/lib/__tests__`. Tirar o `57` do `EASY_IDS` do mobile mantém web verde, jest verde e `tsc --noEmit` limpo — a mesma deriva silenciosa que deixou o `52` errado sobreviver meses.

- source_spec: `_bmad-output/implementation-artifacts/spec-testes-do-web-com-vitest.md`
  summary: `kindForActivity`, `STRENGTH_IDS` e `EASY_IDS` são copiados entre web e mobile, e `GPS_ACTIVITY_IDS` está duplicado em mais dois arquivos — candidatos a subir para `@vitale/shared`.
  evidence: `web/src/app/features/treinos/data/planned-match.ts` e `mobile/src/lib/planned-match.ts` carregam a mesma função (o segundo com um comentário "⚠️ Espelho … manter as duas em sincronia"); `GPS_ACTIVITY_IDS = {13,24,37,52}` aparece em `web/src/app/core/models/activity-types.ts:20` e `mobile/src/lib/workout-types.ts:120`. Nada força paridade entre as quatro cópias.

- source_spec: `_bmad-output/implementation-artifacts/spec-testes-do-web-com-vitest.md`
  summary: Nenhum teste protege a invariante que causou o bug — `GPS_ACTIVITY_IDS` e `EASY_IDS` não podem ter interseção.
  evidence: Recolocar `52` em `EASY_IDS` hoje mantém os 122 testes verdes, porque a checagem de GPS vem antes e o id fica inalcançável. Um caso afirmando a disjunção pegaria a próxima ocorrência; o comentário inline documenta o motivo mas não impõe nada.

- source_spec: `_bmad-output/implementation-artifacts/spec-testes-do-web-com-vitest.md`
  summary: Não há CI nem git hook — nada impede a suíte de voltar a apodrecer.
  evidence: Root `AGENTS.md` afirma "Não há CI nem git hooks"; `ls .github/workflows` não existe. A causa raiz aqui foi um spec escrito em `03225b8` que nunca executou; torná-lo executável sem ligá-lo a nada automático mantém o mesmo modo de falha.

- source_spec: `_bmad-output/implementation-artifacts/spec-testes-do-web-com-vitest.md`
  summary: O target `test` não configura cobertura nem limiar, então não se sabe que fração da lógica os testes tocam.
  evidence: O schema do builder aceita `coverage`, `coverageReporters` e `coverageThresholds`; nenhum foi definido. O problema declarado da spec era lógica "sem cobertura efetiva" — a suíte agora roda, mas segue sem medida.

- source_spec: `_bmad-output/implementation-artifacts/spec-testes-do-web-com-vitest.md`
  summary: `STRENGTH_IDS` contém `35`, que o próprio repositório rotula como **Remo**, sob o comentário "Tipos HealthKit de musculação/força" — mesma classe do bug do `52`, na linha adjacente.
  evidence: `packages/shared/src/fitness/activity-types.ts:14` diz `35: 'Remo'` e `mobile/src/lib/workout-types.ts:229` confirma `// Remo`. Remo classificar como `strength` em vez de `endurance` é decisão de produto, igual à da caminhada — precisa de resposta humana antes de mexer.

- source_spec: `_bmad-output/implementation-artifacts/spec-testes-do-web-com-vitest.md`
  summary: `kindForActivity` devolve `'none'` para ids que o app exibe normalmente — 63 (HIIT), 16 (elíptico), 44 (escada), 82.
  evidence: Todos têm ícone, cor e rótulo em `BASE`, mas nenhum cai em GPS/STRENGTH/EASY, então nunca casam com um treino planejado. Nenhum teste fixa o ramo `'none'`.

- source_spec: `_bmad-output/implementation-artifacts/spec-testes-do-web-com-vitest.md`
  summary: O target `serve` continua em `@angular-devkit/build-angular:dev-server`, o que obriga a manter o pacote legado — que ainda declara peers de karma e jest.
  evidence: `@angular/build` traz o próprio `dev-server` (confirmado no `builders.json`). Migrando `serve`, dá para remover `@angular-devkit/build-angular` inteiro; isso também limpa o peer inválido que `npm ls jsdom` reporta (`jest-environment-jsdom@29.7.0 invalid: "^30.2.0"`). Verificado que hoje `ng serve` não quebra: o dev-server detecta `@angular/build:application` como esbuild-based.

- source_spec: `_bmad-output/implementation-artifacts/spec-testes-do-web-com-vitest.md`
  summary: Os testes unitários são construídos através da config completa do app, arrastando CSS do Leaflet, `src/styles.scss` e `public/` para o bundle de teste.
  evidence: `buildTarget: vitale-web:build:development` emite um `styles.css` de 15,2 kB para specs que não tocam o DOM. Além do tempo desperdiçado, acopla a suíte unitária ao pipeline de assets — um stylesheet global quebrado passa a derrubar os testes.

- source_spec: `_bmad-output/implementation-artifacts/spec-splash-igual-ao-icone.md`
  summary: A marca salta na transição splash nativa → `SplashOverlay`: encolhe e sobe ~50pt quando o JS assume, e fica assim pelo `MIN_SPLASH_MS` inteiro.
  evidence: O storyboard faz aspect-fit na tela cheia, então o glifo do ícone (bbox 188,203–865,861 num canvas 1024) renderiza ~260pt de largura numa tela de 393pt. Em seguida o `SplashOverlay` desenha `OrbeMark size={220}` (~175pt de conteúdo visível) mais o wordmark de 72pt, que empurra a marca acima do centro. Antes do fix o salto existia mas era de um quadrado laranja para a marca — ninguém o leria como continuidade quebrada; agora as duas pontas são a mesma arte em tamanhos diferentes, o que torna o pulo visível. Casar o `size` do overlay com os ~260pt (ou usar `imageWidth` do plugin) fecha o handoff.

- source_spec: `_bmad-output/implementation-artifacts/spec-splash-igual-ao-icone.md`
  summary: A receita do prebuild em sandbox — único jeito sancionado de alterar `mobile/ios/` — não existe em nenhum arquivo versionado.
  evidence: ADR 0009 diz "`mobile/ios/` nunca se edita à mão — a alteração vai pela ferramenta que o gera", mas a ferramenta (`expo prebuild`) apaga o `AppDelegate.swift:32` se rodada no repo. A saída (prebuild numa cópia, transplante só do que mudou) só está registrada neste spec, em `_bmad-output/`. `mobile/AGENTS.md` não menciona splash, prebuild, `ios/` nem assets. O próximo agente esbarra na proibição sem alternativa documentada — e está a um `npx expo prebuild` de matar o HealthKit em background.

- source_spec: `_bmad-output/implementation-artifacts/spec-splash-igual-ao-icone.md`
  summary: ADR 0009 exige bump de `runtimeVersion` em toda mudança nativa; este fix recusou o bump deliberadamente e a ressalva não está registrada em nenhum ADR.
  evidence: O ADR diz literalmente "Mudança nativa exige rebuild e bump do runtime em dois lugares: `Expo.plist` e `app.json`". Troca de asset assado no binário é mudança nativa. O raciocínio (contrato JS↔nativo intacto; bump orfanaria a lane OTA `preview` sem ganho) é defensável mas mora num spec em `_bmad-output/` — o próximo leitor vê só o ADR contrariado. `docs/decisions/` é append-only: cabe um ADR novo que superseda com a ressalva "asset-only não bumpa".

- source_spec: `_bmad-output/implementation-artifacts/spec-splash-igual-ao-icone.md`
  summary: `splash.backgroundColor` e `resizeMode` continuam sem guard, sendo exatamente a mesma classe de drift que o fix acabou de fechar para a imagem.
  evidence: `app.json` declara `backgroundColor: "#FFF7EE"` e `resizeMode: "contain"`; o lado nativo carrega os valores em `SplashScreenBackground.colorset` (255,247,238) e no `contentMode="scaleAspectFit"` do storyboard. Os três são consumidos no mesmo prebuild e dessincronizam do mesmo jeito — editar a cor no `app.json` sem regenerar deixa o colorset antigo, sem nada acusando.

- source_spec: none
  summary: **Fase 3 do piloto BMAD — ciclo completo sobre o Garmin Venu 4**: aba Fitness do mobile lendo do Supabase em vez do HealthKit, mais a cobertura dos dados que o Garmin não escreve no Apple Health (VFC, VO₂max, SpO₂, rota GPS, stream de FC).
  evidence: Adiado em 2026-08-17 por decisão do usuário, ao fim da Fase 2. É a única fase que exercita as 4 fases do BMAD de ponta a ponta — `bmad-deep-recon` → `bmad-product-brief`/`bmad-prd` → `bmad-architecture` + TEA → `bmad-create-epics-and-stories` → `bmad-sprint-planning` → `bmad-build` story a story → `bmad-retrospective`. Ponto de partida técnico já mapeado: `mobile/src/store/fitness.store.ts` e `mobile/src/app/fitness/*` são os únicos lugares que ainda leem HealthKit; `mobile/src/store/activities.store.ts` já é o padrão de leitura do Supabase. Começar em sessão nova — só a fase de Análise enche um contexto. Plano completo em `~/.claude/plans/quero-comecar-a-estudar-fizzy-whisper.md`.

- source_spec: none
  summary: **Fase 4 do piloto BMAD** — criar, com o BMB, uma skill própria que codifique o gate de migration manual do Supabase (gera SQL → mostra → espera aplicação humana → registra em `schema_migrations`).
  evidence: Adiado em 2026-08-17. É a fase curta em que "estudar o framework" vira construir um: `SKILL.md`, arquivos `step-NN-*.md`, o resolver TOML de 4 camadas e os hooks do `bmad-customize`. A regra a codificar já está escrita e verificada em `AGENTS.md` (raiz).

- source_spec: none
  summary: **Fase 5 do piloto BMAD** — decidir a adoção oficial: reescrever a seção de processo do `CLAUDE.md`, definir o destino das 55 specs em `.claude/specs/`, rodar a retrospectiva do piloto e mergear `bmad-pilot` em `main`.
  evidence: Adiado em 2026-08-17. Depende da Fase 3 para ter evidência suficiente — decidir adoção total com base só nas Fases 1 e 2 seria decidir com meio experimento. O plano do Winston em `_bmad-output/planning-artifacts/plano-mover-specs-para-docs.md` já ataca a parte das specs, com um motivo independente: `project_knowledge` do BMAD aponta para `docs/`, que não existe.

- source_spec: `_bmad-output/implementation-artifacts/spec-reescrever-readme.md`
  summary: `CLAUDE.md` está defasado nos mesmos eixos que o README acabou de corrigir — e o README novo aponta para ele como "status snapshot".
  evidence: Diz "Fontes: Geist (sans)" quando `packages/shared/src/constants/tokens.ts:131` define `'Manrope'`; lista 7 rotas quando `web/src/main.ts` declara 20; a seção "Feito ✅" diz "Mobile: 4 telas de tab" enquanto `mobile/src/app/(tabs)/` tem 6, e a própria seção Stack do mesmo arquivo diz "6 tabs". Enquanto isso o README passou a linkar `CLAUDE.md` como referência de status, então a divergência agora é citável.

- source_spec: `_bmad-output/implementation-artifacts/spec-reescrever-readme.md`
  summary: `docs/specs/00-overview.md` descreve um projeto que não existe mais — "Vitale", "Arquitetura de dados (sem backend)", "dados mockados em memória".
  evidence: O arquivo diz "Fase atual: dados mockados em memória (signals no Angular, Zustand no mobile)" e "Fase futura: API REST ou GraphQL". O repo tem 52 migrations em `supabase/migrations/`, 4 edge functions e `profileGuard` em 21 rotas. É o primeiro documento que a tabela de specs do README leva a ler, e desmente o resto do README.

- source_spec: `_bmad-output/implementation-artifacts/spec-reescrever-readme.md`
  summary: `web/src/app/features/auth/auth.routes.ts` é código morto — exporta `AUTH_ROUTES`, que ninguém importa.
  evidence: `main.ts:10-19` declara `login` e `register` inline; um grep por `AUTH_ROUTES` em `web/src` acha só a própria declaração. Quem procurar onde ficam as rotas de auth acha esse arquivo primeiro e conclui que está ligado.

- source_spec: `_bmad-output/implementation-artifacts/spec-reescrever-readme.md`
  summary: O `description` do `package.json` da raiz ainda diz "Vitale — Life organizer with Angular web dashboard and React Native mobile app".
  evidence: Único lugar versionado fora do escopo npm que ainda carrega o nome antigo como marca (o escopo `@vitale/*` é intencional, per ADR/CLAUDE.md). Ficou fora do escopo do spec do README, que proibia tocar em `package.json`.

- source_spec: `_bmad-output/implementation-artifacts/spec-reescrever-readme.md`
  summary: Nada versionado documenta como levar o app ao device nem como publicar as edge functions — os dois caminhos que o CI não cobre.
  evidence: `mobile/eas.json` define development/preview/production com distribuição interna, `mobile/plugins/` tem 5 config plugins (2 para entrega em background do HealthKit) e `supabase/config.toml` marca `connections-ingest` e `strava-oauth` com `verify_jwt = false` (autenticam-se por `x-cron-secret` e por `state` assinado). O README deliberadamente não cobre deploy — o spec listava isso em "Ask First" —, mas o vazio não é preenchido por nenhum outro doc.
- source_spec: none
  summary: Detalhe web de Registros — rota /registros/:id com métricas por período e heatmap clicável (CAP-5/6/7 lado web do SPEC-registros).
  evidence: Split do build de 2026-09-02; mobile-first é constraint do spec e o lado web só consome o núcleo depois de entregue, sem acoplamento reverso.
- source_spec: `_bmad-output/implementation-artifacts/spec-registros-detalhe-mobile.md`
  summary: Extrair nomes de mês/dia pt-BR para módulo único no shared — a story criou a 4ª e 5ª cópias (detail.ts e detalhe.tsx, além de fitness/overview.ts e goals/format.ts).
  evidence: Achado do blind-hunter na revisão de 2026-09-02; duplicação real confirmada por grep, mas o merge das cópias pré-existentes excede o escopo da story.
- source_spec: `_bmad-output/implementation-artifacts/spec-registros-detalhe-mobile.md`
  summary: CI não valida typed routes do Expo Router — gerar .expo/types/router.d.ts antes do tsc no job mobile (ou commitar a declaração), senão pathname inválido passa verde.
  evidence: Verification-gap demonstrou: com router.d.ts ausente (condição exata do CI), push para rota inexistente compila com 0 erros; gap pré-existente que afeta o app inteiro, exposto pela rota nova.
- source_spec: `_bmad-output/implementation-artifacts/spec-registros-detalhe-web.md`
  summary: Heatmap anual de Registros sem rótulos de mês/dia nem legenda marcado/não — achar "aquele dia de março" num grid 53×7 depende só do tooltip; merece um passe de design (mobile idem).
  evidence: Achado do blind-hunter na revisão de 2026-09-02; enhancement de produto, não bug — o spec não pedia rótulos e a decisão de layout pede calibração visual.
- source_spec: `_bmad-output/implementation-artifacts/spec-curva-de-forma-shared.md`
  summary: A curva de forma é só de frequência cardíaca; treino de força (`Treino`, `Lift`) entra como duração × MET independentemente da intensidade e subconta o custo de recuperação.
  evidence: Revisão da etapa 1 apontou que sessão de força gera pouco tempo em zona e muito custo de recuperação; o módulo não tem entrada para carga de força e a limitação foi apenas documentada.
- source_spec: `_bmad-output/implementation-artifacts/spec-curva-de-forma-shared.md`
  summary: Todo módulo do repo tem spec durável em `docs/specs/`, mas a curva de forma só tem o spec de implementação em `_bmad-output`; decidir se ganha `docs/specs/curva-de-forma/` quando a etapa 2 (UI) fechar.
  evidence: Revisão notou que o cabeçalho do teste cita "a matriz de casos do spec" sem nada em `docs/specs/`; o CLAUDE.md lista os specs de feature ali.
- source_spec: `_bmad-output/implementation-artifacts/spec-curva-de-forma-mobile.md`
  summary: A linha "Ver a curva completa" do canvas (tela da curva de forma com a série longa, base e cansaço lado a lado) não entrou — não há tela destino; decidir onde ela mora (`fitness/` ou `saude/`) e desenhá-la antes de ligar o rodapé do cartão.
  evidence: O design aprovado tem a linha no rodapé da Hoje, mas as propostas de tela cheia (Faixa, Ano, Web) ficaram na página "Descartadas" do canvas; a etapa 2 se restringiu ao cartão.
- source_spec: `_bmad-output/implementation-artifacts/spec-curva-de-forma-mobile.md`
  summary: O selo "N DIAS SEM SINCRONIZAR" afirma uma causa que o dado não distingue — `daysSinceLastActivity` conta dias sem atividade, não sem sync; um descanso real de 5 dias com a conexão sadia manda o usuário a Conexões. O lado das Conexões sabe o último sync bem-sucedido e poderia desambiguar (descanso × pipeline parado).
  evidence: Limitação vem da etapa 1 (o núcleo documenta que silêncio e sync parado chegam iguais) e da regra de UX dos 4 dias; a revisão da etapa 2 apontou que o texto do selo escolhe uma das causas. Não é da história do cartão, é do modelo de confiança.
- source_spec: `_bmad-output/implementation-artifacts/spec-curva-de-forma-mobile.md`
  summary: A barreira "nenhuma lista rolável do mobile mostra barra" (`packages/shared/src/architecture.test.ts:268`) lê um genérico de tipo como tag JSX — `useRef<ScrollView>(null)` é acusado de `<ScrollView>` sem `showsVerticalScrollIndicator`; o parser deveria ignorar `<` precedido de identificador (`useRef<`, `Ref<`), como faz com chaves e aspas.
  evidence: Falso positivo reproduzido na etapa 2 (`FormCurveCard.tsx:72`); contornado com `React.ComponentRef<typeof ScrollView>`. Limitação pré-existente da barreira, não do cartão.
- source_spec: `_bmad-output/implementation-artifacts/spec-vfc-intervals.md`
  summary: Os demais consumidores de `'vfc'` (retrospectiva, destaques da semana, tendências 30/90d da Saúde, correlações de gatilho) comparam período contra período na série crua e vão narrar o degrau SDNN→RMSSD como queda fisiológica; nenhum lê `extra.kind` nem conhece a data da virada.
  evidence: A prontidão ganhou baseline filtrada por tipo de medida na revisão da etapa; os outros caminhos não. É trabalho de produto (como anotar a virada num gráfico), não do passo de ingestão.
- source_spec: `_bmad-output/implementation-artifacts/spec-vfc-intervals.md`
  summary: A janela inicial de 120 dias é escolhida por "não existe nenhuma linha `'vfc'` desta fonte"; um usuário cujo Apple Watch cubra todos os dias nunca grava uma, e refaz a busca de 120 dias a cada tick (96×/dia). O conserto certo é estado persistido (coluna `wellness_backfilled_at` em `linked_accounts`), o que pede migration.
  evidence: Apontado por dois revisores. Não afeta o caso real (a VFC do Watch parou em 17/07, então a primeira gravação acontece no primeiro run), mas é desperdício estrutural para instalação nova.
- source_spec: `_bmad-output/implementation-artifacts/spec-vfc-intervals.md`
  summary: A prontidão aceita como "VFC de hoje" a leitura mais recente da janela de 7 dias, que pode ter 6 dias — o cartão apresenta isso como o estado da manhã. Vale para o HealthKit e para o fallback; exigir hoje/ontem (mantendo as antigas só na baseline) é mudança de semântica dos dois caminhos.
  evidence: Revisão da VFC. Comportamento pré-existente do caminho HealthKit, herdado pelo fallback; mudar só um dos dois criaria assimetria pior que o problema.
- source_spec: `_bmad-output/implementation-artifacts/spec-vfc-intervals.md`
  summary: A Hoje passou a carregar `useHealthDailyStore` (um ano de `health_daily`, todas as métricas, paginado) para ler 7 linhas de VFC; uma leitura dirigida (`metric='vfc'`, últimos 7 dias) seria uma requisição pequena. O store é cache compartilhado com Semana, Recuperação e notificações, então a economia depende de decidir quem paga a carga.
  evidence: Revisão da VFC. Custo de rede na tela inicial, não erro; o cartão renderiza sem a tabela e ela chega depois.
- source_spec: `_bmad-output/implementation-artifacts/spec-vfc-intervals.md`
  summary: O fallback da VFC congela numa sessão longa: `useHealthDailyStore.load()` é no-op depois de carregado e nada na Hoje força recarga, enquanto o ingest grava a cada 15 min. Semana e o digest já usam `load(true)`; falta decidir quem força na Hoje sem refazer a busca de um ano.
  evidence: Revisão da VFC, mesmo padrão que a curva de forma resolveu com `load(true)` no foreground — lá o store é barato, aqui não.
- source_spec: `_bmad-output/implementation-artifacts/spec-vfc-intervals.md`
  summary: `supabase/functions` não tem typecheck nem teste em comando nenhum do CI (não há Deno no ambiente); `ingestWellness` é exportada e recebe o `admin` por parâmetro, então um cliente-stub cobriria a sonda, a leitura de precedência e o `catch` best-effort.
  evidence: Achado do revisor de lacunas de verificação. Mitigado em parte nesta etapa (toda a decisão foi movida para o núcleo, que tem 18 checks), mas o corpo da function segue verificado só por leitura.
- source_spec: `_bmad-output/implementation-artifacts/spec-vfc-intervals.md`
  summary: O cartão de prontidão não mostra `coverage` nem a origem da VFC, embora o docblock de `readiness.ts` peça que quem exibe o score avise quando `coverage < 1`; a legenda segue "sono · coração · atividade" e o número vira instrução de treinar ou descansar.
  evidence: Revisão da VFC. Decisão de design (o que mostrar e como), não correção do dado.
- source_spec: `_bmad-output/implementation-artifacts/spec-vfc-intervals.md`
  summary: A aba Saúde do mobile lê VFC só do HealthKit, enquanto a Hoje (fallback) e a web (tabela) passam a ver a do intervals.icu — duas telas do mesmo app discordam sobre a mesma métrica no mesmo dia. `docs/specs/mobile-saude.md` e `docs/specs/readiness-treino/spec.md` ainda descrevem a VFC como métrica exclusiva do Apple Health.
  evidence: Achado de dois revisores. A fronteira do spec limitava a mudança mobile ao cartão de prontidão, então é escopo novo, não desvio.
- source_spec: `_bmad-output/implementation-artifacts/spec-carga-acwr.md`
  summary: Etapa 2 do ACWR/monotonia/strain — nenhuma superfície consome `buildTrainingLoad` ainda; a UI precisa de um texto para cada `null` (`monotonyReason`: crônica zerada, semana constante, semana parada, série curta), cruzar com o `trusted` da curva antes de mostrar faixa, e resolver o caso "voltei de um período parado" (ACWR nulo justamente no salto maior).
  evidence: O spec limitou o escopo ao núcleo; a ADR 0027 lista essas obrigações como consequências, e nenhuma delas tem onde acontecer sem tela.
- source_spec: `_bmad-output/implementation-artifacts/spec-carga-acwr.md`
  summary: As fronteiras de faixa do ACWR foram calibradas na literatura sobre a forma acoplada e são aplicadas ao número desacoplado, que é mais sensível — `risk` acende mais que a taxa de base sugere. Calibrar fronteiras próprias exigiria histórico e método que hoje não existem; por ora está documentado e avisado.
  evidence: Levantado pela revisão da etapa 1 e registrado na ADR 0027. Não é bug: é o limite honesto de herdar limiares de estudos de outro desenho.
- source_spec: `_bmad-output/implementation-artifacts/spec-carga-acwr.md`
  summary: `training-load` e `form-curve` são módulos de feature sem spec durável em `docs/specs/` — o repo lista os specs de feature no CLAUDE.md e os dois só têm o spec de implementação em `_bmad-output`. Decidir se ganham `docs/specs/carga/` quando a etapa 2 fechar.
  evidence: Mesma lacuna já registrada para a curva de forma; o segundo módulo da família a herda.
- source_spec: `docs/decisions/0030-prontidao-nao-pontua-dado-velho.md`
  summary: O sub-score de FC em repouso é `100 − delta×4` com teto em 100, então qualquer leitura no nível da baseline ou abaixo marca 100 — e a baseline de 90 dias tornou isso mais frequente, porque ela inclui um período de forma pior. Em 04/09 a FC de 49 bpm marcava 100 contra a base de 90 dias (51,7) e marcaria 93 contra a de 14 (47,2). É a mesma saturação que a curva do sono acabou de resolver, no componente vizinho.
  evidence: Medido na produção ao conferir a implementação do R2. A ADR excluiu recalibrar pesos e fórmulas do escopo; a `baselineShort` já viaja no componente e é o insumo de uma curva assimétrica, se ela vier.
- source_spec: `docs/decisions/0030-prontidao-nao-pontua-dado-velho.md`
  summary: A web não datou as leituras nem alimenta o componente de carga — `latestFor` não devolve o dia e o ACWR vem das atividades, que o `day-score-card` não carrega. Sem `ageDays` o portão de frescor trata tudo como fresco, que é o comportamento antigo, e a cobertura fica no teto de 0,80. É a única superfície que ainda pode publicar nota apoiada em dado velho.
  evidence: Declarado na ADR 0030 e no docblock de `hasData`. A proposta do R2 limitou a superfície ao mobile.
- source_spec: `docs/decisions/0030-prontidao-nao-pontua-dado-velho.md`
  summary: `rollingBaseline` conta LEITURAS, não dias, e as duas coisas só coincidem em série sem buraco. O adaptador do mobile recorta por data antes de chamar; `dayReadiness` (núcleo, usado pela web e pelo digest) e o `day-score-card` ainda passam a constante direto como contagem. Com um mês sem sincronizar no meio, a janela de "90 dias" alcança bem mais que 90.
  evidence: Achado por um teste que falhou ao afirmar que a baseline curta e a longa diferiam com poucas leituras. Corrigido onde a data existe; registrado onde não existe.
- source_spec: `docs/decisions/0030-prontidao-nao-pontua-dado-velho.md`
  summary: Regularidade de sono — o sinal que falta na prontidão e que a pesquisa apontou. Confirmado viável em 04/09: `mobile/src/lib/health-buckets.ts` já recebe os intervalos `{start, end}` e descarta os horários; gravar `bedAt`/`wakeAt` no `extra` e subir `AGG_VERSION` de 5 para 6 recupera 295 das 308 noites, pelo mesmo mecanismo que o v3 (estágios) e o v4 (`inbed`/`onset`) usaram. Depende de as amostras brutas continuarem no aparelho, e o fuso precisa ser gravado para a regularidade ser em hora local.
  evidence: Investigado a pedido do dono durante o R2 e deixado fora do escopo por exigir migration e backfill de 500 dias.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-onaccent-no-sistema-de-tema.md`
  summary: `ConsistencyCard.tsx:74-79` deriva à mão a MESMA regra que `onAccent` agora possui — argmax de contraste para o primeiro plano sobre o acento cheio —, e o degrau mais quente dela é literalmente `roleColors('orange').accent`, o caso exato do token novo. As duas discordam porque o segundo candidato local é `surface` e o do token é `bgPure`: no `orbe/dark` dão `#1E1A15` a 5,22 contra `#000000` a 6,34. Nada quebrado hoje — as duas são legíveis —, mas é uma segunda implementação de uma regra que o sistema de tema passou a ser dono, e `roleColors('orange').onAccent` já é alcançável daquele arquivo.
  evidence: Achado pela revisão de lacuna de verificação da Story 1.2, que confirmou não haver teste medindo o primeiro plano daquela rampa — `fitness/consistency.test.ts` cobre escala e janela, não cor. Pré-existente: a story não causou nem piorou, só tornou a duplicação visível ao dar dono à regra.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-pacote-por-caderno-com-bases-nomeadas.md`
  summary: `PACOTE_VERSAO` subiu de 1 para 2 e isso não invalida nada — nenhum caminho de leitura consulta `pacote_versao`. `fetchEdicao` filtra só por usuário e período; `precisaErrata` compara só `aggVersionNoMomento`. As edições v1 em produção continuam sendo devolvidas e renderizadas como atuais, sem marcador, embora a forma do pacote e a estrutura do prompt que as produziram tenham sido substituídas.
  evidence: Mesmo padrão do `agg_version_no_momento` da Story 1.1 — coluna de versão sem leitor. Achado pela revisão de lacuna de verificação da 1.3, que demonstrou que reverter a constante para 1 só reprova as duas asserções auto-referentes. Janela limitada: as 7 edições v1 saem na migração da Story 1.9.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-pacote-por-caderno-com-bases-nomeadas.md`
  summary: `semDado` nunca pode ser `true` para Movimento e Rotina. Os dois empurram fatos por `deRecap`, que coage `RecapValue` ausente para `0`, então `atual` é zero e nunca nulo — um período sem nenhuma atividade e sem nenhuma tarefa reporta `semDado: false` com uma página de zeros. Só Sono e Coração, alimentados por `deMetrica`, preservam nulo e conseguem ser marcados vazios. O significado do campo é assimétrico entre os quatro cadernos.
  evidence: Achado pela revisão da 1.3. Ainda não é lacuna viva: o consumidor de `semDado` é o passo 6 do ranqueamento, que nasce na Story 1.7 — mas é ela que vai herdar a assimetria.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-pacote-por-caderno-com-bases-nomeadas.md`
  summary: `FatoTexto` vira armadilha para a regra 1 do verificador no dia em que a Story 1.5 o mostrar ao modelo. Hoje o teste assere que o `72` de "72% pavimentado" NÃO entra no alfabeto numérico, o que está certo enquanto `linhaDeFato` não renderiza `textos`. No instante em que o prompt mostrar a string, qualquer texto que a cite é reprovado como número inventado. As duas formas só são coerentes enquanto os textos ficarem fora do prompt.
  evidence: Achado pela revisão da 1.3, que verificou que `linhaDeFato` hoje não renderiza `textos`. É decisão da 1.5: ou os textos entram no alfabeto, ou o prompt os mostra de forma que a regra 1 não os leia como número.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-o-texto-nomeia-a-base.md`
  summary: A metade da quinta regra renegociada em 09/09 — nome próprio do período nomeando B1, e nome próprio errado como inversão — está provada só em teste sintético. Das 7 edições reais, quatro não citam base nenhuma e as três que citam são semanas, cujo rótulo (`27/07 – 02/08`) não é forma de prosa: `nomesProprios` devolve vazio para elas. `previousPeriodLabel`, `MONTHS_PT` e a acusação por nome errado nunca foram exercitados contra texto verdadeiro.
  evidence: Achado pela revisão de lacuna de verificação da 1.4, que demonstrou que apagar o ramo de `year` de `nomesProprios` deixa a suíte inteira verde. A prosa das edições de mês e ano não declara pares (atual, anterior) — todas escrevem "subiu para X", nunca "de X para Y" —, então dar-lhes bases exigiria fabricar o prior que se quer medir. A lacuna ficou visível na tabela publicada, com asserção prendendo a coluna em zero.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-o-texto-nomeia-a-base.md`
  summary: Semana e trimestre carregam `rotuloAnterior` e são aceitos literalmente, mas não têm detecção de nome errado — `27/07 – 02/08` e `Q1 2026` não são formas que alguém escreva em prosa. Sem essa detecção, nenhuma acusação por nome próprio existe nesses dois tipos de período.
  evidence: Declarado no código e no artefato durante a 1.4. Enumerar variantes que ninguém escreve seria fabricar o léxico; a decisão foi declarar em vez de inventar.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-o-prompt-aprende-as-regras-novas.md`
  summary: A quinta regra trata uma base NEGADA como nomeação dela. "Sem a normal do período, o único contraste é 862" reprova por inversão, com 862 sendo valor de B1 — mas a frase não atribuiu 862 a B3, disse que B3 não existe. TENTADO EM 09/09 E ABANDONADO: a solução ingênua (excluir a menção negada da nomeação) foi implementada, revisada e REVERTIDA, e quem voltar ao assunto precisa saber por quê. TRÊS ACHADOS, todos reproduzidos: (1) o argumento que a sustentava é FALSO — ausência é absolvível por parágrafo e inversão não é, então tirar a marca não converte inversão em ausência, converte em APROVADO sempre que o parágrafo nomear a base certa, que é justamente a prosa que o prompt ensina; medido: "…contra 380 no período anterior. E sem a normal do período: 380." passa calado com o rótulo errado. (2) "base negada não tem valor" é invariante POR FATO, e a negação é ato do TEXTO inteiro: negar B1 falando da nota de sono arranca a menção que absolvia a distância, e texto que passava passa a reprovar. (3) a JANELA_DECISAO de 64 CORTA o negador, então a própria frase que motiva o conserto, com quatro palavras a mais, continua reprovando por inversão. A ÚNICA FORMA QUE SOBREVIVE aos três é um TERCEIRO VEREDITO: menção negada perto do número não absolve nem acusa — obriga nomeação explícita. Isso mexe na mecânica da regra, não no léxico, e pede spec própria.
  evidence: Registrado pela Story 1.5 e reaberto em 09/09 com spec própria (spec-base-negada-nao-e-nomeacao.md, deixada em draft), implementação revertida e branch fix/base-negada-nao-e-nomeacao descartada. GATILHO DECIDIDO PELO DONO: não construir sem evidência. Frequência observada é ZERO — as 7 edições reais não contêm um único "sem", "não há", "nenhum" ou "não existe". A Story 1.15 imprime três edições sob o prompt v3 e passa a responder também: alguma foi recusada por inversão sobre menção negada? Se sim ao menos uma vez, constrói-se na forma do terceiro veredito; se nunca acontecer, esta entrada morre. Custo por ocorrência enquanto isso: uma edição recusada, ~US$0,011, e a reimpressão — falha BARULHENTA, que é preferível ao rótulo errado silencioso que a tentativa ingênua produzia.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-o-prompt-aprende-as-regras-novas.md`
  summary: Rótulo de fato com dígito no nome — `Cerveja 500 ml`, `SpO2`, `Oxigênio (SpO₂)` — é renderizado na linha do fato desde a v1, e o número dentro dele NÃO está no alfabeto. Um texto que cite o hábito pelo nome reprova na primeira regra: `verificarTexto('A cerveja 500 ml apareceu em 12 dias')` acusa `"500" não está no pacote`. Nomes de hábito e de registro são texto do usuário, então o repositório não controla a forma. A Story 1.5 deu guarda de dígito ao `desde` da trajetória e ao `nomeDoFato` do bloco de ausências, mas não à linha do fato, que é de onde o modelo mais copia.
  evidence: Achado com execução pela terceira rodada de revisão da 1.5. PRÉ-EXISTENTE — `- ${f.rotulo}: …` é rendering da v1 e a story não o introduziu nem piorou. O conserto tem custo real: esconder o rótulo tira do modelo o nome do fato, e autorizar o número engorda o alfabeto, que é o que a frente inteira existe para não fazer.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-o-prompt-aprende-as-regras-novas.md`
  summary: O cabeçalho `# ${periodo.rotulo}` leva dígitos não autorizados em semana e trimestre — `# 03/08 – 09/08` põe quatro números fora do alfabeto no topo do prompt. Só datas ISO são mascaradas em `verificarTexto`, e a única defesa hoje é a linha da FORMA "Não repita o intervalo do período", que é prosa e não guarda.
  evidence: Achado com execução pela terceira rodada da 1.5. PRÉ-EXISTENTE (o cabeçalho é da v1) e de alcance limitado: o postal da semana não grava edição (CAP-9), então o tipo mais exposto é o que menos imprime.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-o-prompt-aprende-as-regras-novas.md`
  summary: Numa edição de ANO, B1 e B2 são o mesmo período e o prompt as mostra como duas comparações distintas, com dois números diferentes. `verificar.ts` já sabe disso — o nome próprio do ano nomeia as duas juntas —, então trocar os valores de B1 e B2 sob a frase do outro passa na quinta regra. Só o prompt finge que são comparações separadas.
  evidence: Achado pela terceira rodada da 1.5. A origem é o pacote (`montarPacotes` aceita B2 externa para um ano sem reconciliá-la com B1), não o prompt; consertar aqui esconderia a divergência em vez de resolvê-la.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-o-prompt-aprende-as-regras-novas.md`
  summary: A manchete é a primeira frase do texto, lida SOZINHA na capa e no sumário — mas a quinta regra absolve por parágrafo. Uma primeira frase como "Foram 862 km." passa (o parágrafo nomeia a base adiante) e, extraída, vira um valor de base apresentado como se fosse o do período, sem base nomeada. Nada confere a frase em isolamento.
  evidence: Achado com execução pela terceira rodada da 1.5. É da Story 1.8, que é dona da extração da chamada: é lá que a frase passa a existir como objeto próprio, e é lá que ela pode ser conferida como tal.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-a-camada-de-luz-entra-nos-quatro-pacotes.md`
  summary: A lista `CAUSA` de `verificar.ts` só tem as formas com preposição solta — "por conta de", "em função de", "em razão de" —, e deixa passar as contraídas: "por conta da/dos", "em função da", "em razão dos". "O sono subiu por conta da luz" passa na regra de causa.
  evidence: Achado com execução pela terceira rodada de revisão da 1.6. PRÉ-EXISTENTE — a lista é da v1 e a story não a tocou. Ficou visível agora porque a linha da luz é o primeiro item do cabeçalho que convida a explicar por uma causa de fora. Consertar é alargar a lista, que é mexer na severidade da regra — decisão própria, fora da 1.6.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-a-camada-de-luz-entra-nos-quatro-pacotes.md`
  summary: A terceira regra de `verificar.ts` aceita como ressalva de amostra qualquer ocorrência de "poucos dias" no texto inteiro — "num mês de poucos dias de sol" satisfaz a ressalva de uma correlação fora do portão que não tem nada a ver com a frase.
  evidence: Achado com execução pela terceira rodada da 1.6. PRÉ-EXISTENTE (regex da v1: `/amostra|poucos dias|poucas noites|insuficiente/` sobre o texto todo). A linha de luz convida a frase, mas não a criou. O conserto é escopar a ressalva à vizinhança da correlação citada, como a quinta regra faz com a base.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-a-porta-o-fio-e-o-orquestrador.md`
  summary: A porta não tem prazo — um motor que nunca resolve deixa `ler` pendente para sempre, sem piso e sem evento no anel; cada borda (o `invocar` da 1.10 e da 5.4, a ponte da 5.5) tem de impor prazo próprio e devolver `transitoria`, porque o orquestrador não cancela.
  evidence: Reproduzido na revisão da 5.1 (motor que nunca resolve deixa `ler` pendente depois de 500 ms). A AD-4 põe "timeout" em `transitoria` e a espinha diz que a ponte não cancela, então o prazo é da borda, não do núcleo; na 5.5 o voo único por hash prenderia todos os toques seguintes no pedido travado.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-a-porta-o-fio-e-o-orquestrador.md`
  summary: A causa `preferencia` cobre quatro situações — o usuário escolheu o template, a preferência gravada é ilegível, o `regimeMaximo` é `sem-modelo`, e o padrão filtrado sobrou vazio —, então a tela da 5.5 não sabe dizer por que o escolhido não escreveu; ou a 5.5 distingue pela preferência gravada, ou `resolverCadeia` passa a devolver o motivo junto da cadeia.
  evidence: Reproduzido na revisão da 5.1 para o id ilegível e o teto `sem-modelo`. A causa segue a definição da AD-12 ("a cadeia era só sem-modelo"); o que falta é a procedência, e ela só importa na tela.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-a-porta-o-fio-e-o-orquestrador.md`
  summary: O motor de nuvem da 5.1 manda à `ia-narrar` só `{sistema, usuario, json}` e ignora em silêncio `amostragem`, `guardrails` e `saida.esquema`; na 5.6 o corpo leva os três e a function os honra ou devolve `capacidade` — até lá, a coluna de nuvem da bancada (5.4) não é gulosa, e o relatório tem de dizer isso.
  evidence: Achado da revisão da 5.1. O spec fixou o corpo de hoje de propósito (a function lê só esses três campos), mas `hashDoPedido` inclui a amostragem, então a bancada compara a resposta da nuvem com um pedido que a nuvem não viu inteiro.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-a-porta-o-fio-e-o-orquestrador.md`
  summary: A tabela de status da nuvem manda 400 para `capacidade` (recua sem investigar), mas o 400 de hoje é defeito do cliente (`json_invalido`, `prompt_vazio`); e 404 (function não deployada) cai em `transitoria` + `naoMapeado`, que para a cadeia, quando pelo critério da AD-4 é `indisponivel` — rever as duas linhas na 5.6, junto com o `STATUS_POR_CLASSE`.
  evidence: Achado da revisão da 5.1, com o 404 reproduzido. Nenhum dos dois acontece com descritor testado e function no ar, e para a revista as duas classes terminam no mesmo piso; por isso fica para quando a tabela passar a ser emitida de verdade.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-a-porta-o-fio-e-o-orquestrador.md`
  summary: A guarda (7) só conhece as peças que moram em `ia/`, mas os descritores moram com o domínio — a montagem e a conferência da Saúde do sono vão para `sleep/` na 5.3, e um hospedeiro que as importe para sequenciar por conta própria não aciona a catraca; a 5.3 decide se as peças da leitura entram na guarda.
  evidence: Achado da revisão da 5.1. O escopo `ia/` foi decidido no plano para a catraca nascer em 1 e virar barreira na 1.10; o recorte certo para `sleep/` só existe quando a 5.3 separar o que é peça do que é exibição.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-a-porta-o-fio-e-o-orquestrador.md`
  summary: O resultado de piso por causa permanente não carrega a assinatura, os tokens nem o valor da tentativa recusada, e o nome de rota grava exatamente isso em `route_name_meta` quando recusa (`provedor`, `modelo`, `tokens`, `regiao`, `via`, `justificativa`); a 5.7 precisa desse dado no resultado para passar pela porta sem mudar o que grava — e `grava.recusaEResultado` hoje é declarado e não é lido.
  evidence: Achado da revisão da 5.1, conferido em `routes/nomear.ts`. A 1.10 não precisa (a revista não grava recusa), então a forma pode ser acertada na 5.7, que é o primeiro recurso que grava recusa.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-a-porta-o-fio-e-o-orquestrador.md`
  summary: A conformidade ao `Esquema` não é conferida em lugar nenhum do caminho — nem o `ler` valida o esquema do pedido, nem confere a resposta; cada descritor de saída `esquema` tem de lembrar de chamar `conformeAoEsquema` no `interpretar` e mapear a falha para `saida-invalida`. Avaliar centralizar na 5.7, o primeiro recurso com esquema.
  evidence: Achado da revisão da 5.1. O spec deu a interpretação ao descritor; centralizar muda o contrato do `Descritor`, e só vale a pena com o primeiro consumidor real.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-a-porta-o-fio-e-o-orquestrador.md`
  summary: Nada amarra uma mudança no pedido a um bump de `Descritor.versao`, e o hash, a assinatura e a comparação da bancada dependem disso; quando a 5.2 e a 5.3 registrarem os primeiros descritores, cada entrada do catálogo ganha um golden de `hashDoPedido(montarPedido(fixture), versao)` em `recursos.test.ts`.
  evidence: Achado da revisão da 5.1. Hoje o catálogo nasce vazio e não há pedido real para fixar; o golden só tem alvo a partir da 5.2.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-a-porta-o-fio-e-o-orquestrador.md`
  summary: Cada hospedeiro vai reescrever a mesma normalização do `{data, error}` do `functions-js` (ler o corpo em `error.context` do `FunctionsHttpError`) para montar o seu `invocar` — o mobile na 1.10, a bancada na 5.4, a web depois —; avaliar na 1.10 um normalizador puro no núcleo, com contexto estrutural e teste de contrato, antes de a segunda cópia nascer.
  evidence: Achado da revisão da 5.1. É exatamente o passo que hoje é código morto nas duas cópias do cliente (`edicao-ia.ts`, `route-name.ts`), e a AD-10 existe para impedir a terceira cópia de um cliente da `ia-narrar`.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-a-porta-o-fio-e-o-orquestrador.md`
  summary: O `semComentario` de `architecture.test.ts` apaga comentário por regex e não conhece literal — uma string com `/*` (um glob como `'**/*.ts'`, uma regex) come o código até o próximo `*/` —, e desde a 5.1 ele é compartilhado por mais guardas; trocar por um varredor que respeite literais.
  evidence: PRÉ-EXISTENTE (as duas cópias já tinham a regex); a 5.1 só a subiu para o topo. Nenhum arquivo alvo tem esse literal hoje, então não há falso verde medido, só o risco.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-o-descritor-da-retrospectiva-e-as-listas-com-dono.md`
  summary: O modo de casamento de cada subconjunto de `VOCABULARIO_PROIBIDO` vive só num comentário — `causa` por trecho na revista, os cinco novos por palavra inteira —, e a 5.3 precisa decidir como a Saúde do sono casa `causa` quando o compuser; avaliar pôr o modo no dado ou uma função de violações no dono, antes de nascer o terceiro casador.
  evidence: Achado da revisão da 5.2. Já há dois casadores (`baixo.includes` em `verificarTexto` e `casaPorPalavra` no teste do vocabulário); a spec deixou o casamento dos subconjuntos novos para a 5.3 de propósito.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-o-descritor-da-retrospectiva-e-as-listas-com-dono.md`
  summary: Casando por palavra inteira, os termos no singular deixam passar as flexões — "metas", "setas", "conquistas", "pontuações", "melhoraram", "melhorando", "recomenda", "sugere", "sugestão", "experimentar", "tente dormir" —; o casador da 5.3 ou a lista precisam cobri-las, com teste de violação flexionada.
  evidence: Achado da revisão da 5.2, conferido contra a lista aprovada: nenhuma forma flexionada está nela, e nenhum teste usa uma.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-o-descritor-da-retrospectiva-e-as-listas-com-dono.md`
  summary: "continue assim" está em `conselho` e em `elogio`; o recurso que compuser os dois (a Saúde, na 5.3) conta dois problemas para uma ocorrência — deduplicar ao compor.
  evidence: Achado da revisão da 5.2. As duas fontes são a mesma linha do `SISTEMA`; o teste só proíbe repetição dentro de um subconjunto.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-o-descritor-da-retrospectiva-e-as-listas-com-dono.md`
  summary: O casamento por trecho de `causa` tem falso positivo real na revista — "levou a" casa com "O sono levou algumas noites para voltar ao horário." —, e texto correto reprovado é edição que não imprime.
  evidence: PRÉ-EXISTENTE: `CAUSA` e o casamento são os de antes, e a spec da 5.2 os congelou ("reprova exatamente o que reprovava"). Mexer muda a conferência da revista — decisão para a 1.10/1.11 ou para quando a 5.3 fixar o casador por palavra.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-o-descritor-da-retrospectiva-e-as-listas-com-dono.md`
  summary: A causa `preferencia` ganhou uma quinta situação — a preferência gravada que o recurso não admite (a revista com o aparelho) —, com trilha vazia, igual a quem escolheu o template; a tela da 5.5 continua sem saber dizer por que o escolhido não escreveu.
  evidence: Achado da revisão da 5.2; soma-se ao item da 5.1 sobre as quatro situações de `preferencia`. Preferência não admitida só nasce se o `admite` encolher, mas aí some sem rastro.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-o-descritor-da-retrospectiva-e-as-listas-com-dono.md`
  summary: `format/` tem dois formatadores de número em pt-BR — o `fmtNum` privado de `format/money.ts`, sobre `toLocaleString('pt-BR')`, e o `formatarNumero` de `format/numero.ts`, escrito à mão —, que podem divergir (Intl × manual, o Intl do Hermes no mobile); decidir se um passa a usar o outro.
  evidence: PRÉ-EXISTENTE: os dois já existiam antes da 5.2, que só mudou `formatarNumero` de arquivo; o comentário novo de `numero.ts` diz "com um dono só", o que só vale para a prosa da IA.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-o-descritor-da-retrospectiva-e-as-listas-com-dono.md`
  summary: `formatarNumero` escreve "−0,0" para negativo que arredonda a zero (`-0.04`, 1 casa) e "NaN"/"Infinity" literais para valor não finito; a 5.3 vai interpolar por ele em texto que o usuário lê — tratar o zero negativo e recusar não finito, com teste.
  evidence: PRÉ-EXISTENTE: é o corpo de antes, e a spec da 5.2 exigiu "toda saída formatada é a mesma de antes". Conferido pelo revisor com as entradas citadas.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-o-descritor-da-retrospectiva-e-as-listas-com-dono.md`
  summary: A AD-12 manda o recurso que grava declarar "como a assinatura vira colunas", e nenhuma story tem isso; a 1.10 grava `prompt_versao`/`pacote_versao` a partir de `versaoDoDescritor` — decodificar no núcleo (`ia/imprimir.ts`), nunca reescrevendo as constantes de hoje no hospedeiro, que a guarda (7) vai proibir de importá-las.
  evidence: Achado da revisão da 5.2. A codificação (`PROMPT_VERSAO × 1000 + PACOTE_VERSAO`) mora em `ia/retrospectiva.ts` e a decodificação só existe no teste; a espinha lista a declaração no descritor, e o `epics.md` não a dá a nenhuma story.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-o-descritor-da-retrospectiva-e-as-listas-com-dono.md`
  summary: A `Cadeia` marcada não carrega o recurso para o qual foi resolvida; a conferência por elo da 5.2 pega regime e `admite`, mas não o destinatário — a cadeia da Saúde, com a nuvem que o usuário escolheu para ela, passa no `ler` da revista e manda a edição para aquele provedor.
  evidence: Achado da revisão da 5.2. Exige erro de ligação no hospedeiro; fechar pede carimbar o recurso na cadeia em `resolverCadeia` e conferir no `ler`, o que muda a assinatura das duas — decisão para a 5.5, quando nasce o primeiro hospedeiro com duas cadeias.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-o-descritor-da-retrospectiva-e-as-listas-com-dono.md`
  summary: Texto de recusa sem número nem termo de causa ("Não posso ajudar com isso.") passa em `verificarTexto` e seria impresso como edição permanente — a revista declara recusa como não-resultado, mas nada detecta recusa em texto.
  evidence: PRÉ-EXISTENTE: a conferência de hoje também não detecta, e a spec da 5.2 registrou "Não detecta recusa". Relevante para a 1.10/1.11, que imprimem e gravam.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: Ninguém produz lápide ainda. O núcleo sabe recebê-la — mapa `LAPIDES` em `period/cadernos.ts`, `FatoLapide` e `EntradaPacote.lapides` em `ia/pacote.ts`, a seção "Métricas que pararam de chegar" no prompt, o passo 5 em `ia/ranqueamento.ts` —, mas `lapides` é opcional e o celular monta `{ resumo, agora }` (`mobile/src/app/retrospectiva/index.tsx:285`). Nenhuma edição em produção carrega lápide, e as quatro mortes de 2026 (respiração 10/07, VO₂max 14/07, SpO₂ 16/07, anéis 17/08) seguem narradas como silêncio. Falta o DETECTOR: a última medida de cada uma das quatro em `health_daily` e um limiar de silêncio que separe métrica morta de semana sem relógio.
  evidence: A Story 1.7 proíbe detectar métrica morta, consultar banco ou fixar limiar (Never) — "quem morreu não é decidido aqui: a lápide chega pronta na entrada". O limiar é decisão de produto e tem dois lados que a 1.7 não mediu: quantos dias de silêncio viram morte, e o que acontece quando a métrica volta por outra fonte (a VFC voltou pelo intervals.icu depois de parar no Apple Watch em 17/07). O lugar natural é a camada de dados ou a sequência da impressão (1.10), nunca o núcleo puro.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: Com os seis passos como estão, Movimento abre 24 dos 39 meses fechados, e Sono e Coração não abrem nenhum. O afastamento é o `|deltaPct|` cru, e as métricas não oscilam na mesma escala — contagem de atividade oscila dezenas de por cento de um mês para o outro, FC de repouso oscila 1–5% —, então o mesmo 5% é ruído num caderno e manchete no outro. É o R-35 aparecendo cedo, nas palavras da spec.
  evidence: Simulação registrada nas Design Notes da spec 1.7, sobre os 39 meses fechados de produção (17 com dois cadernos ou mais), a mesma que calibrou o N = 7. Não foi refeita aqui (sem acesso a produção nesta sessão). A correção natural — afastamento contra a oscilação típica da própria métrica — muda o passo 1, que é contrato ("Ask First" da 1.7). Vai para a 1.15, que imprime três edições e é portão humano: se o dono ler a ordem como "sempre Movimento", o passo 1 se reabre ali, com dado.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: A espinha da arquitetura da revista (`_bmad-output/planning-artifacts/architecture/architecture-Orbe-revista-2026-09-08/ARCHITECTURE-SPINE.md`) põe `ordenarCadernos` em `period/cadernos.ts` — no Structural Seed e na linha CAP-7 da tabela de capacidades. Ela mora em `ia/ranqueamento.ts`.
  evidence: O ranqueamento precisa de `cadernoVazio`, que é do pacote, e `ia/pacote.ts` já importa `period/cadernos.ts` em runtime: lá, a função fecharia um ciclo de import. Em `ia/` ela nasce sob a barreira do núcleo de IA do `architecture.test.ts`. A spec 1.7 registra a divergência como "correção pendente na espinha, que não se edita aqui" — a correção é do documento, e é pequena.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: A regra "a lápide lidera se a última medida cai no período" colide com a latência do detector que ainda não existe. A morte só pode ser declarada depois de N dias de silêncio; uma edição impressa logo no fechamento não vê como morta a métrica que parou nos últimos N dias, e a edição seguinte já a recebe como antiga — que nunca lidera. Com N grande, "capa uma vez só" vira "capa nunca" para toda edição impressa em dia.
  evidence: Achado pela revisão cega e pela de borda da 1.7, independentes. Medido em 10/09 na conversa da 1.7: com limiar de 30 dias, a edição de julho impressa em 1º de agosto não teria lápide nenhuma (SpO₂ calava havia 16 dias, VO₂max 18, respiração 22). A entrada do detector acima não menciona a interação, e ela é a pergunta que o detector tem que responder junto com o limiar — por exemplo, a lápide ser "do período" na primeira edição impressa depois da detecção.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: A lápide escrita no MESMO parágrafo de um valor de B1 sem nome faz a quinta regra aprovar em silêncio: o nome do mês da data ("parou de chegar em 10 de julho") é lido como nomeação de B1 numa edição de agosto e absolve o número. Só a FORMA manda a lápide em parágrafo próprio; nada mecânico cobra.
  evidence: Documentado por teste na própria 1.7 ("o parágrafo próprio é o que protege", `verificar.test.ts`), e apontado pela revisão cega e pela de borda. É falso negativo, não falso positivo — o texto passa com rótulo errado. Consertar é mexer na severidade de `verificar.ts`, que é "Ask First" da 1.7: por exemplo, mascarar a frase de lápide antes da quinta regra, como as datas ISO já são mascaradas.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: A cobertura do Coração compara o período consigo mesmo — `coberturaDe(nCoracao, dias, nCoracao, dias)` —, então ela nunca é desigual e o portão do passo 2 nunca barra o Coração por cobertura. E o prompt imprime "No período comparado: 29 de 31 dias" para um lado que ninguém mediu.
  evidence: PRÉ-EXISTENTE (linha da v1, anterior à 1.7). Ficou relevante agora porque o portão lê a cobertura. A 1.7 criou o `nAnterior` que permitiria o conserto, mas trocar os números da Cobertura muda o alfabeto e o prompt — "Ask First". Enquanto isso, a amostra por fato (`min(n, nAnterior)`) ainda barra o fato de amostra pequena.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: O período anterior é tratado como se tivesse o tamanho do atual — `coberturaDe(cs.noites, diasNoPeriodo, cs.noitesAnterior, diasNoPeriodo)` e a escala `1 / diasNoPeriodo` dos passos por dia. Março contra fevereiro diz "de 31 dias" para fevereiro, e os passos por dia de fevereiro saem subestimados.
  evidence: PRÉ-EXISTENTE (v1). Apontado pela revisão cega da 1.7. `previousPeriodStartISO`, que a 1.7 criou, torna o conserto simples — mas ele muda números que vão ao prompt e ao alfabeto.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: O portão de nascimento cobre só hábito e registro. A mesma amputação do lado anterior atinge tarefas, compras e gasto (quando o módulo começou no meio do período anterior) e o esporte de uma fonte conectada no meio dele — e produz "Compras +150%" com amostra ≥ 7.
  evidence: Apontado pela revisão cega da 1.7. A spec aprovada fixou o nascimento só para hábito e registro, os dois que têm `createdOn`; os outros não têm data de nascimento no `RetroSummary`. Hoje compras e gasto chegam zerados, então o custo é zero até haver dado.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: A cobertura de noites do caderno Sono barra também a nota de sono, que é subjetiva e tem amostra própria (dias com nota): num mês de 27 × 14 noites e 30 × 30 notas, a nota não pode liderar o caderno.
  evidence: Apontado pela revisão cega da 1.7. É o contrato ("a cobertura do caderno"), então não é defeito da story; é refinamento para o piloto da 1.15, que mede se a ordem convence.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: Quando o afastamento vem de B3 ou B2, o portão ainda usa a amostra do lado de B1 — a amostra da normal e do ano anterior não é conhecida. Uma comparação com a normal bem amostrada pode ser barrada por um mês anterior ralo, e o inverso.
  evidence: Apontado pela revisão de borda da 1.7. Hoje nenhum hospedeiro passa B2/B3, então a regra ainda não decide nada. Quando `BaseExterna` ganhar valor, ela precisa ganhar amostra junto.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: Duas perguntas para a 1.9 e a 1.10 sobre `metrica_lider`: (1) `liderDoCaderno` devolve `null` para o caderno que só tem lápide ou que nenhum fato passou no portão, e a coluna nasce `not null`; (2) no caderno forçado pela lápide, o "líder" que ela devolve é o fato numérico mais forte do caderno, não o que lhe deu a posição — e é ele que a tira do anuário vai medir.
  evidence: Design Notes da spec 1.7 ("Entrega para a 1.9 e a 1.10") e revisão cega. A AD-17 manda carimbar "a chave da métrica que forneceu o afastamento — o passo 1"; para o caderno forçado, o passo 1 não deu a posição, e a espinha não diz o que carimbar.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: A simulação que calibrou o N = 7 (e mediu "Movimento abre 24 de 39") vive só no scratchpad da sessão de 10–11/09. O número está nas Design Notes e na doc de `AMOSTRA_MINIMA`; o método e os agregados não estão em lugar nenhum do repositório.
  evidence: Apontado pela revisão cega da 1.7. Os agregados são dado pessoal (nomes de hábito e de registro), então não vão para o repositório como estão. O que cabe é o método: as consultas mensais de `activities`, `habit_logs`, `registro_logs`, `daily_ratings` e `health_daily`, e a regra de amostra usada — para o piloto da 1.15 poder refazer a conta.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: Nas contagens (hábito, registro, atividades, sessões), a amostra do portão é o menor dos dois lados (`menorLado` em `ia/pacote.ts`, `Math.min(atual, anterior)`), então um colapso real nunca lidera. "Cerveja 20 → 0 dias" (−100%) tem amostra 0; "20 → 3 atividades" num mês de lesão tem amostra 3. O portão foi desenhado contra efeito grande sobre base fina ("+136% sobre 11 dias"). Ele barra junto a queda grande e verdadeira, em que o lado fino é o próprio acontecimento, e isso é notícia.
  evidence: Apontado pela segunda rodada de revisão da 1.7. Nenhum teste da matriz cobre queda para zero. A raiz é a mesma do domínio de Movimento (entrada acima): afastamento cru mais piso fixo. A saída que cobre os dois casos é medir ruído em vez de fixar piso. Pelo acaso de Poisson, 1 → 3 dias de registro é ruído (|ln 3| = 1,10 < 2·√(1 + 1/3) = 2,31), e 20 → 3 atividades não é (1,90 > 1,24). Mudar o portão é "Ask First" da 1.7 e decisão do dono, então vai para o piloto da 1.15, junto com a entrada do domínio de Movimento.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: O portão de nascimento só olha a B1: `comparavel` (`nascidoAntes` em `ia/pacote.ts`) compara `createdOn` só com o início do período ANTERIOR. Quando B2 e B3 ganharem valor, um hábito criado em maio/2026 passa no portão e é comparado com o mesmo mês do ano anterior, ou com uma normal calculada sobre dias em que ele não existia. Como o afastamento usa a base mais confiável (B3, depois B2, depois B1), o portão passa a olhar a base errada no dia em que Rotina ganhar B2.
  evidence: Apontado pela segunda rodada de revisão da 1.7. Ainda não tem efeito: registros, hábitos e notas não têm B2 nem B3 antes de mai–jun/2027 (`bases-e-ranqueamento.md`), e hoje nenhum hospedeiro passa B2/B3. Complementa a entrada sobre a amostra de B2/B3: quando `BaseExterna` ganhar valor, ela precisa ganhar junto a amostra e a data de nascimento da fonte.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: Nada impede que a lápide e o dado vivo da mesma métrica estejam no mesmo pacote, por exemplo "saturação de oxigênio parou de chegar em 16 de julho" ao lado de um SpO₂ medido no período. `validarLapides` confere catálogo, calendário, fim do período e caderno, mas não olha `metricas`. Por definição, a última medida é a última: se a métrica voltou, a lápide está errada.
  evidence: Apontado pela revisão cega e pela de borda na segunda rodada da 1.7. Hoje o caso não acontece, porque nenhuma linha de saúde da retro é uma das quatro métricas com lápide (os `HEALTH_SPECS` dos dois apps são sono, vfc e fcRepouso). A regra cabe ao detector: ele não declara morta a métrica que voltou, inclusive por outra fonte. É o caso real da VFC, que parou no Apple Watch em 17/07 e voltou pelo intervals.icu. Quando o detector existir, o núcleo pode recusar o par.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: A lápide antiga não tem prazo. O núcleo aceita toda lápide com última medida até o fim do período, sem limite de baixo, como a spec manda. Se o detector repassar todas as mortes conhecidas, toda edição futura de Movimento e de Coração que não for vazia vai fechar com "saturação de oxigênio parou de chegar em 16 de julho de 2026 — antes deste período", em parágrafo próprio pela FORMA, e isso vale até em 2028. A doc usa contra a lápide que lidera para sempre o argumento "depois da terceira o leitor para de ver", e ele vale igual para a lápide do pé.
  evidence: Apontado pela revisão cega e pela de borda, independentes, na terceira rodada da 1.7. `lapidesPorCaderno` em `ia/pacote.ts` só corta pelo fim do período. A spec (Always) diz "entra no pacote se a última medida ≤ fim do período" e deixa ao detector decidir quem chega ("a lápide chega pronta na entrada"). Hoje ninguém produz lápide, então o custo é zero até o detector existir. A pergunta é do detector, junto com o limiar e a latência (entradas acima): por quanto tempo uma morte antiga continua sendo repassada, seja um número de edições, uma janela ou só enquanto o caderno não voltar a ter a métrica.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: Nada impede a tela de importar `ordenarCadernos` por caminho profundo (`@vitale/shared/src/ia/ranqueamento`). A 1.7 só garante que a função não sai pelo barril (`ranqueamento.test.ts` cobra a ausência no `index.ts`). O cabeçalho de `ia/ranqueamento.ts` atribui à Story 1.9 a barreira que varre `mobile/src` e `web/src`, e isso não está escrito em nenhum outro lugar.
  evidence: Apontado pela revisão cega na terceira rodada da 1.7. A spec proíbe exportar pelo `index.ts` porque a ordem é de impressão (1.10), nunca de render (1.9): uma tela que recalculasse a ordem reordenaria em silêncio toda edição fechada no dia em que um peso mudasse. A barreira nasce naturalmente com a coluna `posicao` da 1.9, como um teste em `architecture.test.ts` que recusa `ia/ranqueamento` fora de `packages/shared`. Esta entrada existe para levar isso até lá.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-o-ranqueamento-do-miolo-e-a-lapide.md`
  summary: Em produção, a perna de cobertura do portão nunca barra o Sono. O celular monta `{ resumo, agora }` (`mobile/src/lib/edicao-ia.ts`) sem `coberturaSono`, então `cobertura.sono` fica nula, e caderno sem cobertura não é desigual. A linha "Sono atrás (27 × 14 noites)" da matriz da 1.7 depende de uma entrada que ninguém envia. Com o `nAnterior` que a 1.7 passou a preencher, o Sono disputa só pela amostra de dias (14 ≥ 7). Pelo mesmo motivo, a ressalva da regra 8 sobre noites está parada.
  evidence: Apontado pela revisão cega na terceira rodada da 1.7. Conferido: nenhum arquivo de `mobile/src` menciona `coberturaSono`. É PRÉ-EXISTENTE no hospedeiro: `EntradaPacote.coberturaSono` é opcional desde a 1.4, o celular nunca o passou, e a 1.7 não toca tela (Never). O conserto cabe à sequência da impressão (1.10), que monta a entrada: passar as noites dos dois lados liga o portão de cobertura e a ressalva. Com a cobertura do Coração, que se compara consigo mesma (entrada acima), as duas pernas de cobertura do portão estão hoje sem efeito.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-3-a-leitura-da-saude-do-sono-sem-modelo.md`
  summary: Na "última" noite, a `/sono/saude` dos dois apps roda `periodScore` sobre uma noite só (horário sempre 2, "± 0 min", base que inclui a própria noite) e a leitura usa `nightScore` — no arquivo de teste, 9 de 30 noites caem em caso diferente do que a tela mostra; a 5.5, que troca o cálculo da tela por `entradaDaSaude`, muda o que a tela mostra nessas noites e precisa mostrar isso ao dono antes.
  evidence: Achado da revisão da 5.3, sobre a decisão aprovada no checkpoint (a noite usa `nightScore`, como o cartão da aba e a medição de 10/09).
- source_spec: `_bmad-output/implementation-artifacts/spec-5-3-a-leitura-da-saude-do-sono-sem-modelo.md`
  summary: As stores de sono dos dois apps carregam só 90 dias de notas (`SONO_WINDOW_DAYS`), então em 12m, no ano e em janelas antigas a percepção da tela sai de notas truncadas e a da bancada, de todas — caso e hash podem divergir calados; a 5.5 carrega as notas da janela que `entradaDaSaude` lê.
  evidence: PRÉ-EXISTENTE: `mobile/src/store/sono.store.ts:17` e `web/src/app/features/sono/data/sono.store.ts:22`; a 5.3 recorta as notas à janela, mas não consegue repor as que o app não buscou.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-3-a-leitura-da-saude-do-sono-sem-modelo.md`
  summary: O cartão Sono da Retrospectiva na web mistura separadores depois da vírgula da percepção — o bloco da Saúde mostra "3,4/5 · N notas" e a "Nota média" do mesmo cartão segue `toFixed(1)`, "3.4/5"; o mobile já troca por vírgula.
  evidence: PRÉ-EXISTENTE: `web/src/app/features/retrospectiva/components/sleep-retro-card.component.ts:167`; a 5.3 não pode tocar `web/`.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-3-a-leitura-da-saude-do-sono-sem-modelo.md`
  summary: A guarda (7) libera todo nome `descritor*`, e por isso um app pode chamar `descritorDaSaudeDoSono.montarPedido`/`conferir`/`montarFrase` direto — a segunda sequência pedido → modelo → conferência que a AD-2 proíbe — sem a guarda ver, porque ela lê nomes de import, não acesso a membro; apertar antes de a 5.5 ligar o botão.
  evidence: Achado da revisão da 5.3. Hoje nenhum app importa o descritor; o primeiro consumidor é a 5.5.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-3-a-leitura-da-saude-do-sono-sem-modelo.md`
  summary: A percepção do período arredonda o fato e os pontos por caminhos diferentes — o fato por `toFixed(1)` (dentro de `formatarNumero`), os pontos por `Math.round(noteAvg * 10) / 10` —, e média de notas como 29/20 aparece "1,4/5" enquanto os pontos contam 1,5.
  evidence: PRÉ-EXISTENTE (o `toFixed` já estava em `sleep/score.ts` antes da 5.3; o `formatarNumero` o mantém). Conferido na revisão 2 da 5.3: 320 pares k/n, com n ≤ 400 notas, divergem; o conserto é um arredondamento só para os dois, e muda fato de tela nos dois apps.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-3-a-leitura-da-saude-do-sono-sem-modelo.md`
  summary: A lista `causa` só tem o verbo no singular do pretérito ("causou", "levou a", "resultou em", "provocou") — "causaram", "levaram a", "resultaram em" e "provocaram" passam na revista (por trecho) e na Saúde (por palavra).
  evidence: PRÉ-EXISTENTE: a lista é a `CAUSA` de antes dos motores, e a 5.3 não pode mudá-la (a conferência da revista fica como está); pôr as flexões muda o que a revista reprova e pede o `verificar.test.ts` revisto junto.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-3-a-leitura-da-saude-do-sono-sem-modelo.md`
  summary: `filterByRange` com `'ultima'` anda por período, não por dia, então dois períodos no mesmo `wake_day` fazem dois passos do ◀ mostrarem a mesma data com contagens diferentes — e a leitura da Saúde herda isso, porque ela é a fórmula da tela.
  evidence: PRÉ-EXISTENTE (`sleep/ranges.ts`, usado desde a CAP-8 pelo cartão da aba). Achado da revisão 3 da 5.3 e conferido no reexport de 12/09: 1 dos 293 dias tem dois períodos. Fechar pede decidir qual período é "a noite" do dia — decisão de produto, não de agente.
- source_spec: `_bmad-output/implementation-artifacts/spec-ensaio-da-migracao.md`
  summary: As migrations nunca declaram GRANT, então um banco feito só delas deixa o app sem acesso a tabela nenhuma. Produção funciona porque nasceu com os privilégios padrão antigos do Supabase (ALL para `anon`, `authenticated` e `service_role` em toda tabela nova do `public`, EXECUTE em toda função); o CLI atual cria o banco com o padrão novo, que não expõe nada à API. É o contrário do que a ADR 0011 promete ("um `db reset` ou um ambiente novo reproduz").
  evidence: Medido no ensaio da migração em 11/09/2026: com as 67 migrations aplicadas num Postgres 17.6 local, o login do dono funcionou e a REST devolveu `permission denied for table edicoes_ia`; as ACLs divergiam em 57 linhas de tabela e 27 de função. A única exceção declarada é o REVOKE de `linked_account_secrets` (`20260711120000_linked_accounts.sql:76`). O ensaio replica o modelo de produção depois do reset; declarar os GRANTs nas migrations é decisão do dono (postura de privilégio, e a 1.9 é a única migração do Épico 1) — ele escolheu adiar.
- source_spec: `_bmad-output/implementation-artifacts/spec-ensaio-da-migracao.md`
  summary: Cinco desvios reais entre produção e migrations, e o `check-schema-drift.sh` pega um. Ele acusa por nome a tabela `health_daily_vfc_backup_20260904`; é cego para o default de `user_preferences.blur_intensity` (100 em produção, 50 na migration — ele compara nome de coluna, nunca o default), para a função `rls_auto_enable()` e o gatilho `ensure_rls` (ele não olha funções nem gatilhos), e para os dois schemas de extensão que só o ensaio mediu: `pg_cron` em `pg_catalog` e `pg_net` em `public` em produção, contra `extensions` no banco feito das migrations.
  evidence: Comparação de catálogo do schema `public` (colunas com tipo e default, constraints, índices, políticas, hash de função, trigger, view e gatilho de evento) entre produção e o banco local do ensaio, em 11–12/09/2026. O backup é resto da limpeza de VFC de 04/09; `rls_auto_enable` é da plataforma, e é o gatilho `ensure_rls` que liga RLS sozinha em tabela nova do `public` em produção — o que vale saber ao escrever a migração da 1.9, que cria tabela. Nenhum dos três toca o que a 1.9 muda, e o ensaio os lista como divergências conhecidas.
- source_spec: `_bmad-output/implementation-artifacts/spec-ensaio-da-migracao.md`
  summary: O ensaio aplica a candidata numa consulta simples e para em ~128 KiB, que é o teto do argumento de um processo — um artefato de COMO o ensaio aplica, não de como produção recebe. Ninguém sabe qual é o teto real do corpo da Management API, então existe uma faixa de tamanho em que a migração é inensaiável aqui e talvez perfeitamente aplicável lá, sem caminho alternativo documentado.
  evidence: Apontado pela revisão cega da primeira rodada do ensaio (12/09/2026). Medir o teto da API exigiria mandar um corpo grande para produção, que é escrita — fora do que o ensaio pode fazer. As migrations do repositório hoje têm no máximo poucos KB, então não morde; morderia numa migração de dados com muitos literais. O caminho, se acontecer: partir a candidata e ensaiar as partes, sabendo que aí a atomicidade do conjunto não é a mesma.
- source_spec: `_bmad-output/implementation-artifacts/spec-ensaio-da-migracao.md`
  summary: A paridade do ensaio ainda não compara parâmetros de sequência (início, incremento, dono), `comment on` de tabela e coluna, e o conteúdo de linha das tabelas sem dado no ensaio. O banco do ensaio carrega só as 7 edições, então migração de DADOS (14 das 67 migrations do repositório fazem `update`) roda contra tabela vazia e o retrato antes × depois não prova nada sobre ela.
  evidence: Apontado pelas revisões cega e de lacunas de verificação na primeira rodada do ensaio (12/09/2026). O que entrou agora foi tipo (enum e domínio), view materializada, `force row level security` e dono, mais um hash agregado por tabela. Carregar dado real de outras tabelas para o banco local é decisão de produto — é dado pessoal saindo de produção para uma segunda cópia —, e a 1.9 não precisa: ela mexe em `edicoes_ia` e cria `edicoes_capa`. Vale reabrir quando uma migração de dados de verdade for ensaiada.
- source_spec: `_bmad-output/implementation-artifacts/spec-ensaio-da-migracao.md`
  summary: Duas ferramentas que o ensaio mostrou faltar e que ele não constrói: um passo que registre em `supabase_migrations.schema_migrations` a versão aplicada à mão pela Management API (hoje é instrução no README, e esquecer deixa a migration "pendente" para sempre e um `db push` futuro a re-executa), e lint de shell no CI, que hoje roda só os três workspaces de JS.
  evidence: Apontado pelas revisões cega e de lacunas de verificação na segunda rodada do ensaio (12/09/2026). O registro é escrita em produção, que o ensaio nunca faz por contrato — cabe à 1.9, que é quem aplica. O lint de shell pega exatamente a classe de defeito que as duas rodadas acharam ("guarda que não guarda"), mas acrescentar job ao CI é decisão de dono e mexe no `.github/workflows/ci.yml`, que já está vermelho por outro motivo.
