# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed

# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created, ready for development
#   - in-progress: Developer actively working on implementation
#   - review: Implementation complete, ready for review
#   - done: Story completed

# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed

# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed

# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when its first story starts (via build's sprint sync)
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates the next story after the previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; the status view surfaces open ones

# PORTÃO DE PRONTIDÃO — 08/09/2026: CONCERNS (não PASS), e mesmo assim seguiu.
# ============================================================================
# 26 das 27 stories são implementáveis como estão. A exceção é a 3.3, e ela é
# declarada, não descoberta. Ver as anotações nas linhas das stories.
#
# ORDEM DE ATAQUE decidida em 08/09, contra a ordem numérica:
#   1º  1.1  — sozinha, antes de qualquer outra. Defeito VIVO em produção e
#              precede o backfill por contrato: fazer depois vira backfill do
#              backfill, em ~1.744 linhas.
#   2º  preparar o ensaio da migração (banco local) — pré-requisito da 1.9, e
#              trabalho independente que roda em paralelo.
#   3º  1.9  — só com o ensaio de pé.
#
# O test design por risco vive em skills/test-artifacts/test-design-epic-1.md:
# 42 riscos pontuados, 19 altos, 3 no teto. É ele que sustenta a ordem acima.
#
# CORRECT-COURSE — 10/09/2026: os motores de IA entram nesta sprint (Épico 5).
# ============================================================================
# Proposta: _bmad-output/planning-artifacts/sprint-change-proposal-2026-09-10.md
# A ordem passa a ser:
#   · 1.7, 1.8 e o ensaio da migração seguem como estavam. 5.1 → 5.2 → 5.3 (F0,
#     núcleo sem modelo) correm EM PARALELO: sem deploy, sem tela, sem banco.
#   · 5.4 (F1, bancada) depois de 5.1 e 5.3. O MARCO A dela — orquestrador em
#     modo medição, colunas sem modelo e nuvem — é portão da 1.10. O MARCO B —
#     a coluna do aparelho, macOS 27 a partir de 14/09 — NÃO segura a revista.
#   · 1.9 como estava. A 1.10 só depois de 1.9, 5.1, 5.2 e a 5.4 no marco A.
#   · 5.5 (F2, ponte + botão) depois da 1.9, em BUILD PRÓPRIO — nunca no da
#     migração. Entre 5.5 e 1.10 a ordem é livre: quem chegar primeiro cria
#     mobile/src/lib/motores/.
#   · 5.6, 5.7 e 5.8 (F3–F5) ERAM da próxima sprint. O dono as trouxe para esta:
#     a 5.6 foi reaberta em 17/09 e está `done`; a 5.7 começou em 21/09 (ver as
#     notas das entradas). A 5.8 (Core AI) segue em `backlog`, com 4 riscos
#     abertos. O plano original desta linha não vale mais — vale o status abaixo.
generated: 09-08-2026 23:20
last_updated: 21-09-2026 21:30
project: Orbe
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts
development_status:
  epic-1: done
  # ▲ COMEÇA AQUI. Risco 9 — defeito vivo em produção hoje: a edição grava null,
  # precisaErrata devolve false para nulo, e NENHUMA edição é elegível a errata.
  # Precede o backfill. Cuidado do R-02: tornar a coluna not null SEM tornar o
  # campo TS obrigatório troca o defeito silencioso por 436 falhas de escrita.
  # Tentativa 1 (08/09) REVERTIDA por loopback de revisão: as quatro passagens
  # obrigatórias fechavam a omissão mas deixavam o VALOR ERRADO passar — trocar
  # AGG_VERSION por PACOTE_VERSAO na tela mantinha tudo verde e carimbava 1.
  # Abordagem renegociada em 09/09: upsertEdicao lê a constante e carimba sozinho,
  # o valor não viaja. Status regredido de propósito — 'review' seria mentira,
  # porque não há implementação a revisar.
  # Tentativa 2 (09/09) IMPLEMENTADA: constante em packages/shared/src/constants/
  # agg-version.ts, upsertEdicao carimba, as três passagens do mobile sumiram,
  # barreira de dono único + edicoes-ia.test.ts. As cinco suítes verdes e as três
  # provas negativas rodadas e revertidas. Falta a linha real em produção.
  # FECHADA em 18/09/2026 — a linha real existe: os 20 cadernos impressos desde
  # 16/09 (agosto, julho, junho e as semanas de 24/08 e 07/09) têm
  # agg_version_no_momento = 9, nenhum nulo. Conferido em produção.
  1-1-agg_version-vira-vocabulário-do-núcleo: done
  # 1.2 a 1.8 FECHADAS em 18/09/2026, por decisão do dono: são núcleo, sem tela
  # própria, e toda edição impressa desde 16/09 passou por elas (a sequência da
  # 1.10 monta o pacote, cobra a base, ranqueia e extrai a chamada).
  1-2-onaccent-no-sistema-de-tema: done
  1-3-o-pacote-fala-por-caderno-com-bases-nomeadas: done
  1-4-o-texto-é-obrigado-a-nomear-a-base: done
  1-5-o-prompt-aprende-as-regras-novas: done
  1-6-a-camada-de-luz-entra-nos-quatro-pacotes: done
  1-7-o-ranqueamento-do-miolo-e-a-lápide: done
  1-8-a-chamada-sai-de-uma-função-só: done
  # Risco 9, o mais alto da frente. ENTREGA ÚNICA: a migração e o JS que a lê
  # não podem sair em momentos diferentes (AD-15) — instância única. HÁ OTA
  # (corrigido em 10/09: runtime 1.0.5, canal preview), mas o update só vale no
  # SEGUNDO lançamento e, publicado antes da migração, quebra do outro lado.
  # É a ÚNICA migração do Épico 1: coluna que a AD-12 dos motores pedir entra
  # aqui, e o CHECK de motivo_de_parada só muda junto com a guarda (6) da 5.1.
  # O raio é o DOBRO do que a story descreve: upsertEdicao quebra junto com
  # fetchEdicao (achado do test design), então um build antigo falha ao GRAVAR,
  # não só ao abrir.
  # FECHADA em 13/09/2026 — a janela da AD-15 foi executada e o dono conferiu no
  # iPhone. Produção: 68 migrations, a versão 20260912120000 registrada, e as
  # nove conferências da forma nova verdes. A semeadura pela própria
  # edicao_imprimir gravou os três cadernos em posicao 1, 2 e 3 — com
  # metrica_lider NULO no de Coração, o caso da lápide — e foi desfeita depois de
  # o dono olhar a tela. É a evidência humana que o test design exigia (T-2), e o
  # único exercício em produção do caminho de escrita que a 1.10 vai usar.
  # O roteiro da janela, o aplicar.sh e o conferir-bundle.py ficaram em
  # implementation-artifacts/revista-1-9/; a seção 6 de lá é leitura obrigatória
  # antes da próxima janela (três armadilhas de BUILD custaram uma hora, e o
  # banco não teve parte nenhuma nisso).
  # Duas dívidas nomeadas ficam para a 1.10 e já estão no deferred-work: a
  # conferência antes de gravar e a leitura de AGG_VERSION no ponto de montagem
  # da carga do RPC. A terceira é a frase provisória da tela.
  1-9-a-migração-e-a-leitura-da-forma-nova: done
  # MUDOU DE ESCOPO em 10/09 (AD-13 dos motores): cliente do orquestrador, duas
  # portas (buscar e gravar), só origem 'motor' grava. Só depois de 1.9, 5.1, 5.2
  # e a 5.4 no marco A. A chave fica a mesma — é ela que liga a story à issue.
  # FECHADA em 17/09/2026 — a escrita voltou pelo núcleo (ia/imprimir.ts), com
  # três barreiras novas e o botão mínimo no cartão, e o dono conferiu a
  # impressão no iPhone. O build de entrega saiu do Xcode 27, que só abre o app
  # com o ciclo por UIScene (fix/uiscene-xcode-27, PR própria, entra antes).
  1-10-a-sequência-da-impressão-sobe-para-o-núcleo: done
  1-11-a-rota-da-revista-e-os-estados-da-edição: done
  # FECHADA em 18/09/2026: as faixas foram aprovadas em tela. O veredito da
  # lápide NÃO é desta story — ela é invisível até existir detector de métrica
  # morta, e o julgamento vai junto com ele (deferred-work).
  1-12-os-cadernos-desenhados: done
  # FECHADA em 18/09/2026 — a capa com foto e o véu consertado (PR #519) vistos
  # pelo dono no iPhone.
  1-13-a-capa-na-edição-com-o-véu-medido: done
  # FECHADA em 18/09/2026 — aprovada em tela e na main (PR #522).
  1-14-o-sumário-e-a-rolagem-ancorada: done
  # ORDEM: entra DEPOIS da 1.14 e ANTES da 1.15 — as duas mexem na mesma tela, e
  # o piloto é onde três capas são lidas de verdade. Pedida pelo dono em 18/09,
  # olhando a primeira capa impressa (julho/2026): ver a foto expandida, saber
  # por que ela foi escolhida (com quando, em que atividade e a rota descrita) e
  # poder trocá-la. CARREGA MIGRAÇÃO — a segunda do Épico 1: `motivo` e
  # `foto_activity_id` em edicoes_capa, janela pela AD-15.
  # FECHADA em 18/09/2026 — na main (PR #524), a migração aplicada pelo dono na
  # janela (69 migrations, versão registrada) e "tudo ok" no iPhone; julho já foi
  # trocada por ele. AGOSTO NÃO TEM CAPA (impresso em 16/09, antes da 1.13), e o
  # app não oferece reimpressão de caderno já impresso — resolver antes da 1.15.
  1-16-a-capa-aberta-e-a-troca: done
  # ⚠ PORTÃO HUMANO — NENHUM AGENTE FECHA ESTA STORY.
  # A entrega de dev é imprimir três períodos nomeados (agosto/2026, julho/2026 e
  # um mês de 2023) e pô-los na frente do dono. O julgamento é dele, lendo. Um
  # loop autônomo que marcar isto como 'done' está errado por construção.
  # O que ela bloqueia: o Épico 2 imprime 53 períodos sobre esta aposta, e o
  # Épico 3 constrói o anuário sobre a metrica_lider que ela valida.
  # GATILHO ACRESCENTADO EM 09/09 — a 1.15 responde uma pergunta a mais, e ela é
  # medição, não julgamento de gosto: ALGUMA DAS TRÊS EDIÇÕES FOI RECUSADA POR
  # INVERSÃO SOBRE UMA MENÇÃO NEGADA DE BASE ("sem a normal do período" perto de
  # um valor de outra base)? Se sim ao menos uma vez, constrói-se o terceiro
  # veredito da quinta regra; se nunca acontecer, a entrada correspondente do
  # deferred-work.md morre. A frequência observada hoje é ZERO — as 7 edições do
  # prompt v2 não contêm um único negador —, e foi por isso que o conserto foi
  # adiado em vez de construído no escuro. Ver deferred-work.md.
  # FECHADA em 18/09/2026 PELO DONO, lendo agosto e julho/2026 no iPhone: "a ordem
  # está ok". Impressos: agosto/2026 (o Sono apagado e reescrito para ganhar capa —
  # opção A dele), julho/2026, setembro e outubro/2023. A MEDIDA de 16/09 deu ZERO
  # chamadas com número de comparação sem a base — a aceitação fica. Os dois
  # limítrofes ("subiu para 85", "subiu para 71 ms": número atual, direção sem
  # base) NÃO contam, por decisão dele. Custo: ~7,3 mil tokens de entrada e ~0,8
  # mil de saída por edição de quatro cadernos.
  # O que o piloto NÃO viu, e virou story antes da 2.3: as lápides (nada as
  # produz — a 2.7, o detector) e o zero no lugar da ausência em 2023 (a 2.6).
  # A ordem mensal saiu IGUAL em junho, julho e agosto (Movimento, Rotina,
  # Coração, Sono) — ele a achou certa; o ranqueamento varia nas semanas.
  # GATILHO DO NEGADOR: os textos novos nomeiam a base NEGADA em quase todo
  # caderno ("sem a normal do período"), então a frequência deixou de ser zero —
  # e NENHUM caderno do piloto apareceu reprovado (resposta do dono, 18/09). A
  # entrada do deferred-work morreu, como o gatilho mandava.
  1-15-o-piloto-de-três-edições: done
  epic-1-retrospective: optional

  epic-2: backlog
  # Encolhe se a 5.4 chegar antes: quem chegar primeiro cria o workspace scripts/.
  2-1-o-quarto-workspace-e-a-barreira-que-o-enxerga: backlog
  # Do lado do modelo, só injeta `invocar` (AD-14 dos motores).
  2-2-o-script-imprime-uma-edição-e-ela-é-idêntica-à-do-telefone: backlog
  # 2.6 e 2.7 entram ANTES da 2.3 — nasceram no piloto (1.15, 18/09/2026): a
  # impressão em massa espalharia o zero no lugar da ausência por 2023–2025, e
  # sairia sem lápide nenhuma. A 2.7 tem três decisões do dono antes do código
  # (limiar, latência e prazo da morte antiga — deferred-work).
  2-6-o-que-ainda-não-era-registrado-não-vira-zero: backlog
  2-7-o-detector-de-métrica-morta: backlog
  2-3-a-impressão-em-massa-dos-períodos-fechados: backlog
  2-4-a-parede-de-capas: backlog
  2-5-silenciar-um-caderno: backlog
  epic-2-retrospective: optional

  epic-3: backlog
  3-1-o-postal-da-semana: backlog
  3-2-o-anuário-do-ano: backlog
  # ⚠ NÃO PRONTA PARA DESENVOLVIMENTO — a única das 27, e de propósito.
  # Metade dos critérios de aceite não existe: forma, posição e tratamento visual
  # dependem de uma passagem de bmad-ux ADIADA deliberadamente para depois das
  # três edições-piloto (desenhar o destaque de um fato que ninguém viu
  # renderizado é projetar a partir do documento).
  # A story PODE MORRER: se a luz na prosa já bastar, ela some e o FR6 fica
  # coberto só pelo Épico 1. A evidência que ela exige antes de fechar não é um
  # teste — é a decisão entre rodar a UX ou matá-la.
  3-3-o-destaque-de-luz-no-trimestre: backlog
  epic-3-retrospective: optional

  epic-4: in-progress
  4-1-o-instante-da-lua-cheia-entra-na-efeméride: review
  4-2-o-teste-lunar-sob-protocolo-e-lua_execucoes: backlog
  4-3-a-barreira-do-hash-incondicional-e-offline: backlog
  4-4-a-página-da-lua: backlog
  epic-4-retrospective: optional

  # ── Épico 5: os motores — entrou em 10/09 por correct-course ──────────────
  # A ordem real está no cabeçalho; aqui fica em ordem numérica.
  # FECHADO em 21/09/2026: o dono deu o veredito das seis em review. O épico
  # entregou o que ele redefiniu como foco em 19/09 — a ponte e a escolha do
  # modelo em Configurações. São quatro motores escolhíveis por recurso:
  # template, modelo da Apple (AFM 3 Core Advanced), nuvem e peso aberto (Core
  # AI). Nenhum modelo virou padrão: a cadeia da Saúde continua no template, e o
  # nome de rota segue na nuvem por herança.
  epic-5: done
  # F0 — núcleo sem modelo. Sem deploy e sem tela. Em paralelo com 1.7–1.9.
  # FECHADAS em 13/09: na main desde 11–12/09, e agora exercitadas no aparelho —
  # o marco A da 5.5 levou este núcleo à tela da /sono/saude, e o dono conferiu
  # no build da janela da 1.9. A cadeia padrão da Saúde continua [sem-modelo]:
  # a nuvem está habilitada, não imposta, e entra por escolha no seletor.
  5-1-a-porta-o-fio-e-o-orquestrador: done
  5-2-o-descritor-da-retrospectiva-e-as-listas-com-dono: done
  5-3-a-leitura-da-saúde-do-sono-sem-modelo: done
  # F1 — dois marcos. O A era portão da 1.10, e FECHOU: a bancada rodou no Mac
  # em 12/09 sobre 295 noites de produção (nuvem aprovada em 22 de 22, zero
  # frase igual à do template, mediana de 13,6 s). O PORTÃO HUMANO foi cumprido
  # — o dono leu o relatório e fixou o limiar na ADR 0050 (aprovação ≥ 90%, os
  # sete casos nos dois alcances, nada idêntico ao template, mediana ≤ 20 s).
  # O marco B — a coluna do aparelho — espera o macOS 27 e vive na 5.5.
  5-4-a-bancada-no-mac: done
  # F2 — dois marcos. O A está na main desde 12/09 (a4ddf33) e no iPhone: o
  # botão Ler no cabeçalho da /sono/saude e o seletor de motor, SEM a ponte.
  # A story só fecha com o marco B — a ponte Swift on-device-engine —, que
  # espera o macOS 27 (14/09) e o risco aberto do Xcode 26.6, e exige BUILD
  # PRÓPRIO: nunca o mesmo de uma migração.
  5-5-a-ponte-o-botão-ler-e-a-escolha-do-motor: done
  # 5.6 REABERTA em 17/09/2026 por decisão do dono: sem dependência de nenhuma
  # outra frente (nem da Revista, nem de marco do próprio Épico 5) — corre em
  # paralelo. Worktree própria em ../life-organizer-wt-motores-5-6, branch
  # feat/motores-5-6. Falta o bmad-build/bmad-create-story numa sessão nova
  # para virar story de fato; até lá o status segue backlog.
  # GUARDA DE COLISÃO: checado em 17/09 — nenhum arquivo da 1.13 (em
  # construção, worktree própria) toca ia/ nem motores/; sem sobreposição de
  # arquivo. O único ponto de contato real é packages/shared/src/ia/motor.ts,
  # que a 1.10 (já done) já importa pela porta única — mudar essa story para
  # SÓ ADICIONAR implementação nova de Motor, nunca alterar a assinatura do
  # tipo existente, ou quebra quem já mergeou. Rodar
  # `pnpm --filter @vitale/shared lint && test` antes de mergear.
  # FECHADA em 18/09/2026 — mergeada na main (PR #521, 4906ff3). O veredito do
  # dono foi o merge: com a lista vazia não há nada a conferir em tela, e o
  # código novo do app vai junto no próximo build, sem build próprio.
  # Oito gates verdes. A revisão adversarial (3 camadas) achou e consertou 11
  # patches, sem loopback — nenhum achado tinha raiz no spec congelado. Os três
  # graves: a BANCADA não tinha adotado a injeção do MotorId (media o modelo
  # padrão e rotulava a coluna com o nomeado — corromperia o portão da ADR 0050);
  # um 200 sem a chave `motores` virava lista vazia autoritativa em vez de null;
  # e `catalogoDoRecurso`, o fio que leva a lista fundida às duas leituras reais,
  # não tinha teste nenhum. 6 achados foram para o deferred-work.
  # FICA COM O DONO, fora da story: o deploy da function (runbook em
  # motores-5-6/deploy-da-lista.md — `supabase functions deploy ia-narrar`, SEM
  # definir o secret). Até lá a produção roda a ia-narrar antiga, e nada quebra
  # em nenhum sentido. A lista fica VAZIA, por decisão do `Never` do spec; o
  # epics.md dizia o contrário. Um 2º modelo na lista é decisão futura dele e
  # pede, nesta ordem, a tabela de regime do provedor, uma rodada da bancada e
  # só então o secret.
  5-6-vários-motores-de-nuvem: done
  # ▼ F4–F5. A 5.7 COMEÇOU em 21/09/2026, na worktree life-organizer-wt-motores-5-7
  # (branch feat/motores-5-7, saída de dbb8df7). O dono decidiu no mesmo dia que a
  # nuvem continua o padrão do nome de rota **por herança**, sem medição — a
  # medição do recurso na bancada foi para deferred-work.md.
  # CONSTRUÍDA em 21/09/2026, revisão de três camadas rodada (11 consertos, sem
  # loopback). Dois vereditos do dono no mesmo dia: a nuvem segue padrão por
  # herança, e a resposta cortada volta a ser tentada em vez de marcar a pedalada
  # (emenda da matriz congelada, registrada no Spec Change Log). Falta ele
  # instalar e conferir: pedalada sem nome ganha nome, e "Nome de rota" aparece
  # escolhível em Configurações → Motores.
  # CONFERIDA NO IPHONE em 21/09/2026, 11:52: o seletor lista "Nome de rota", a
  # escolha do dono é respeitada (o modelo do aparelho respondeu primeiro, e a
  # conferência reprovou a proposta dele — "via repete a região" — gravando a
  # recusa COM provedor, modelo, tokens e justificativa, que é a dívida que esta
  # story pagou), e com a nuvem a mesma pedalada de 27/07 saiu "Aller à Hal",
  # idêntica ao que o caminho antigo produzira 43 min antes, com os MESMOS 1.160
  # tokens de entrada — a prova de que o pedido não mudou.
  5-7-o-nome-de-rota-pela-porta: done
  # ESCOPO REDEFINIDO pelo dono em 21/09, depois da sonda no Mac: a 5.8 passa a ser
  # **a ponte do Core AI**, não o recurso. Ele autorizou subir o alvo mínimo para
  # iOS 27 ("o app é só meu") e NÃO quer peso embarcado por tamanho — mas escolheu
  # embarcar o menor (SmolLM2-135M, 260 MB) só nesta story, como prova, com a
  # dívida de tirá-lo na seguinte. O risco é o empacotamento: o CocoaPods não
  # declara dependência de pacote Swift. Medições que sustentam o recorte:
  # o qwen3-1.7b aprova 97% e acerta 10% nas 135 rotas; o 4b não coube no disco.
  # CONSTRUÍDA e revisada em 21/09/2026 (21 consertos, sem loopback). O
  # empacotamento venceu por uma QUARTA saída — casca fina com `internal import`,
  # cuja .swiftinterface não cita o pacote da Apple —, e o Metal Toolchain não foi
  # preciso: como recurso de pod estático, os pesos são copiados por rsync. Build
  # do iPhone verde com o Core AI dentro. O peso aberto é bloqueado fora da Saúde
  # do sono, que não grava. Falta o portão do dono: instalar e ver texto sair.
  5-8-core-ai: done
  # NASCEU da medição da 5.8, em 21/09. Escopo cortado pelo dono no mesmo dia: só
  # a regra do artigo, que tem prova de corpus (os 135 nomes aprovados usam só
  # `le` e `la`, ambos franceses). A regra da grafia FICOU DE FORA — `Hal` é o
  # nome francês de `Halle`, `Halle` não tem alias nenhum, e a regra reprovaria um
  # nome que a nuvem produziu e ele aceitou. Está no deferred-work com os números.
  # CONSTRUÍDA e revisada em 21/09/2026 (9 consertos, sem loopback). Os dois graves:
  # a regra reprovava o artigo mesmo sem região — e sem região o molde nem o lê —,
  # o que viraria ~98 recusas permanentes indevidas; e "La" com maiúscula passava
  # na conferência mas sumia da frase, porque o molde compara exato. A normalização
  # foi para a leitura (`lerRespostaDoModelo`), não para o pedido.
  5-14-o-artigo-na-lingua-do-nome: done
  # 5.9 REABERTA em 17/09/2026: é o marco B que saiu da 5.5 "por desenho"
  # quando ela fechou em 16/09 (commit 6bcb3fc) — ninguém tinha story própria
  # pra ele. Os portões de SO que o bloqueavam (macOS 27, Xcode 27, iPhone no
  # 27) estão confirmados nesta máquina: xcodebuild 27.0 (build 27A266a),
  # Swift 6.4, macOS 27.0. Sem risco aberto na espinha da arquitetura — ao
  # contrário da 5.8/Core AI, que segue com 4 riscos não resolvidos no
  # Deferred (licença/abordagem indecisa entre alvo mínimo iOS 27, vendorizar
  # apple/coreai-models ou conformidade própria; Xcode 27 fora da imagem EAS
  # da SDK 57 — só build local; memória por tamanho; pesos sem guardrail da
  # Apple num recurso de saúde) e por isso NÃO foi reaberta junto.
  # Worktree life-organizer-wt-revista-1-13 (a de node_modules e iOS já
  # instalados; a wt-motores-5-9 vazia foi removida em 19/09), branch
  # feat/motores-5-9.
  # GUARDA DE COLISÃO: checado em 17/09 — nenhum arquivo da 1.13 (em
  # construção, worktree própria) toca ia/, motores/ nem
  # mobile/modules/on-device-engine/. Dois pontos de contato reais: (1)
  # packages/shared/src/ia/motor.ts e orquestrar.ts, que a 1.10 (já done) já
  # importa pela porta única — só adicionar, nunca mudar a assinatura
  # existente; (2) BUILD PRÓPRIO já exigido pela story original (nunca o
  # mesmo de uma migração) — estende-se a nunca compartilhar build/instalação
  # no iPhone com outra story rodando em paralelo, pelo mesmo motivo da AD-15.
  # 5.11 NASCEU em 19/09/2026 da medição da 5.10 (opção A do dono) e trata só a
  # FORMA do pedido da Saúde — a conferência não muda. CONSTRUÍDA em 19/09/2026
  # e deixada sem push por decisão do dono (a aprovação da ADR 0050 era da v1).
  # MEDIDA em 21/09/2026, nos dois motores, sobre a mesma amostra de 23 janelas:
  #   · aparelho (AFM 3 Core Advanced, iPhone): v1 0 de 23 → v2 11 de 23 (47,8%),
  #     com as reprovações por marcador caindo de 36 para 12;
  #   · nuvem (nuvem:padrao): 23 de 23, mediana 10,1 s e pior caso 18,4 s
  #     (a v1 tinha dado 22 de 22 e mediana 13,6 s em 12/09);
  #   · idênticas ao template: 0 de 23 — a condição 3 da ADR 0050 segue limpa.
  # RESSALVA da amostra: ela tem 12 das 14 combinações de caso × alcance. Faltam
  # `sem-contagem/noite` e `medidas-insuficientes/período`, que NÃO TÊM NENHUMA
  # JANELA no acervo — não é falha da amostra. As 12 presentes foram todas
  # aprovadas pela nuvem.
  # A v2 traz junto a MEDIDA DE CÓPIA DO EXEMPLO (`copiaDoExemplo`, na régua do
  # núcleo desde a 5.13): no modelo pequeno do Mac boa parte das "aprovadas" era
  # o exemplo do pedido devolvido, e a condição 3 só compara com o template.
  5-11-o-pedido-da-saude-para-o-modelo-pequeno: done
  # 19/09/2026 — o DONO redefiniu o foco: o Épico 5 é a PONTE e a escolha do
  # modelo em Configurações; a Saúde do sono é só o recurso de teste. A 5.9
  # começa agora, com o pedido v1 (a v2 da 5.11 fica parada na branch dela até o
  # resultado no iPhone). Escopo desta: o modelo do sistema (disponível, variante
  # AFM 3 Core / Core Advanced, janela), listado de forma simples — o dono revisa
  # a tela com UX depois.
  # CONSTRUÍDA em 19/09/2026: o Engine.swift virou módulo Expo (cola sem lógica,
  # nome OnDeviceEngine), o seletor mostra a variante e a janela do modelo do
  # sistema, o aparelho escreve a Saúde do sono, e a tela de desenvolvimento tem o
  # teste descartável do PCC (emenda na ADR 0047). Revisão em 3 camadas: 21
  # patches, sem loopback. NO IPHONE (1.0.6, 19/09): o aparelho dele tem AFM 3
  # Core Advanced com janela de 8.192; a ponte escreve e a conferência reprova
  # por posição de marcador. O botão do PCC FECHAVA o app — "Missing entitlement:
  # com.apple.developer.private-cloud-compute" (entitlement gerenciado da Apple) —
  # e o experimento foi APAGADO, com o veredito na ADR 0047. Runtime 1.0.7.
  5-9-a-ponte-swift-on-device-engine: done
  # 5.13 NASCEU em 19/09/2026 do teste da 5.9 no iPhone: o aparelho do dono tem
  # AFM 3 Core Advanced (janela 8.192) e o Mac só AFM 3 Core — a bancada do Mac
  # não mede o modelo dele. A tela de desenvolvimento passa a medir a mesma
  # amostra no próprio iPhone, com a mesma régua e o hash de cada pedido.
  # CONSTRUÍDA em 19-21/09/2026: a régua da bancada (amostra, linha, medidas,
  # palavras) subiu para packages/shared/src/bancada/ e o Mac passou a usá-la de
  # lá — os 8 relatórios salvos reconferem sem divergência (`bancada/reconferir.ts`).
  # A tela de desenvolvimento mede a amostra no aparelho, com portão de notas,
  # freio, previsão e o texto de cada janela. Revisão em 3 camadas: 20 patches,
  # sem loopback — o pior era a composição morar na tela, provada por uma cópia.
  # Falta o dono instalar e rodar "Medir a amostra" com a tela acesa.
  5-13-a-amostra-no-iphone: done
  # O ÉPICO TEM DE DISPONIBILIZAR TODOS os modelos que o dono PODE usar
  # (decisão dele, 19/09): o do sistema (5.9) e os abertos do Core AI (5.8, que
  # por isso fica no épico). O Private Cloud Compute ficou FORA no mesmo dia: do
  # Mac ele diz `available`, mas o pedido falha com ModelManagerError 1046 — é o
  # acesso que a Apple dá só a app publicado no Small Business Program. O dono
  # decidiu deixá-lo de fora se não puder usar; ver deferred-work.
  # 5.10 SEPARADA DA 5.9 em 19/09/2026, por decisão do dono, e ENTRA ANTES DELA
  # (a ordem do épico: F1 mede antes de F2 ligar). É a metade do Mac do marco B:
  # nasce o Engine.swift, a CLI Swift o compila sem cópia, as guardas (3) e (4)
  # nascem com ele, e a bancada ganha a coluna do aparelho e a sonda de
  # fidelidade — o relatório que o dono lê antes de a 5.9 gastar build. A 5.9
  # fica com a ponte no app (cola do Expo, motorPara, runtimeVersion, a amostra
  # de 20 pedidos por hash no iPhone). Número novo para não renomear a #515.
  # CONSTRUÍDA em 19/09/2026 — Engine.swift nasceu (fora do app, sem ser
  # linkado), a CLI local o compila sem cópia, a bancada ganhou a coluna do
  # aparelho, a sonda (descritor) e as quatro medidas da ADR 0050 sem veredito.
  # Revisão em 3 camadas: 24 patches, sem loopback. Falta o dono ler o relatório
  # da medição real (acervo de 12/09) — é ele que decide se a 5.9 vale o build.
  # FECHADA em 19/09/2026 com a decisão do dono sobre o relatório: aparelho 3 de
  # 22 (13,6%), mediana 1,6 s, reprovando pela FORMA (rótulo e dois-pontos, mais
  # de uma frase, número por extenso, marcador ausente); sonda 12 de 12. A 5.9
  # não vale o build ainda — antes, uma story curta ajusta o pedido da Saúde
  # para modelo pequeno e mede de novo as duas colunas (opção A dele).
  5-10-a-coluna-do-aparelho-na-bancada: done
  epic-5-retrospective: optional
