---
scope: Migração @vitale/web de Angular 21 para Angular 22
purpose: build-substrate + relatório HTML didático (v18 vs v20 vs v22) para incentivar migrações
altitude: epic
updated: 2026-09-02T15:24
---

- (version) Angular 22.0 lançado 03/06/2026; exige TypeScript >=6.0 e Node >=22 (Node 20 fora; 26 suportado). Fontes: release notes v22.0.0 GitHub + blog.ninja-squad.com 2026/06/03
- (version) Estáveis na v22: Signal Forms, resource()/rxResource()/httpResource(), @angular/aria, @Service decorator, injectAsync(). FetchBackend vira default do HttpClient. strictTemplates default.
- (version) Breaking v22: OnPush vira changeDetection padrão (migração adiciona Eager a quem não declara); paramsInheritanceStrategy default 'always'; canMatch exige currentSnapshot; ?. em template retorna undefined; removidos ComponentFactoryResolver, checkNoChanges, provideRoutes, Hammer.js, builders experimentais web-test-runner/jest
- (constraint) Baseline: @vitale/web em Angular 21, TS ~5.9, zone.js ~0.15 no polyfill (NÃO zoneless), Vitest via @angular/build:unit-test, rotas flat em main.ts, 100% standalone+OnPush via schematic, 81 arquivos com signal/computed, sem HttpClient (supabase-js), sem SSR
- (constraint) Impacto nulo verificado por grep: canMatch=0, Hammer/ComponentFactory/checkNoChanges/provideRoutes=0, reactive forms=0, HttpClient=0, rotas children=0. Impacto real: 4 arquivos com *ngIf/*ngFor legado, 10 arquivos ngModel, 17 serviços providedIn:'root', polyfill zone.js
- (decision) AD: alvo Angular 22.x + TS 6.x + Node >=22; caminho ng update @angular/core@22 @angular/cli@22 com migrações automáticas. Previne: bump manual de package.json sem os schematics
- (decision) AD: zoneless entra no mesmo epic — provideZonelessChangeDetection() no bootstrap, polyfill zone.js sai do angular.json e a dep sai do package.json. Regra: mudança de estado visível passa por signal; nada de depender de zone p/ CD. Previne: componente novo que só atualiza com zone.js presente
- (decision) AD: com OnPush default na v22, código novo OMITE changeDetection (o default já é OnPush) e o schematic do angular.json remove a linha; os existentes ficam como estão (sem diff em massa). A migração automática de Eager não produz nada aqui (todos declaram OnPush)
- (decision) AD: formulário novo usa Signal Forms (estável na v22); os 10 editores ngModel migram quando tocados, não em big-bang. Previne: três estilos de formulário convivendo sem regra
- (decision) AD: os 4 arquivos com *ngIf/*ngFor legado migram para control flow via ng generate @angular/core:control-flow dentro do epic (deprecados desde v20)
- (assumption) [ASSUMPTION] @Service: adotar para serviço root NOVO; os 17 @Injectable(providedIn:'root') existentes migram quando tocados. Cosmético, reversível
- (assumption) [ASSUMPTION] Node local/CI >=22 disponível; conferir .github/workflows antes do merge
- (assumption) [ASSUMPTION] TS 6.0 compila supabase-js 2.x e typings do leaflet sem ajuste; risco baixo, validado no ng update
- (direction) Entregável extra pedido: relatório HTML didático comparando v18/v20/v22 (com v21 como baseline), voltado a incentivar outras equipes a migrar; exemplos de código reais do Orbe (rotas, config, formulários, CD)
- (decision) AD: paramsInheritanceStrategy 'always' aceito como default (rotas flat, sem children); rota aninhada futura já assume herança de params. Previne: leitura de param quebrada ao aninhar rotas depois
- (decision) AD: testes seguem no runner Vitest do @angular/build; zone.js/plugins/vitest-patch proibido (o app vai zoneless). Previne: teste que reintroduz zone por conveniência
- (event) Reviewer gate: rubric walker + lente adversarial aplicados. Fixes: passo zero ng config cli.packageManager pnpm (ADR16); engines raiz >=20 -> >=22; smoke manual como portão do zoneless + rebase de branches abertas; 'tocar' definido no AD-5 e ordem AD-6 antes de zoneless; schematic sem changeDetection no mesmo PR do update; AD-11 docs-sync (AGENTS/CLAUDE no mesmo PR); AD-12 remove @angular/animations (zero uso, deprecado v20.2); AD-13 shared sobe TS ~6.0 no fecho (mobile já está em 6.0.3 — fato corrigido); contagens corrigidas (87 componentes, 80 arquivos control flow); nota de namespace AD-n local
- (constraint) CI ci.yml já roda Node 22 nos 3 jobs; engines da raiz ainda >=20 (sobe no PR do update). fakeAsync/waitForAsync: zero uso nos 11 specs. trigger() de animations: zero uso real (único match é método de store)
- (question) Pendente da lente de verificação web: status real do @Service na v22 (Ninja Squad lista como novidade; rubric walker leu como experimental no changelog) — decide AD-8 e a seção 6 do relatório HTML
- (version) Lente de verificação web: @Service era dev preview na 22.0, estável na 22.1 (ago/2026); patch corrente 22.1.3. Alvo do epic ajustado para ^22.1. Demais fatos confirmados em fonte primária sem ressalva
- (event) spine finalized
- (version) Relatório final da lente web (substitui a prévia): zoneless é default desde a v21 para TODOS — o bootstrap do Orbe não pede provideZoneChangeDetection, logo o app JÁ roda zoneless com zone.js inerte; AD-3 reescrito como remoção de peso morto. Node range oficial ^22.22.3 || ^24.15.0 || ^26.0.0; patch corrente 22.1.4 (27/08); control flow: 69 arquivos (não 80). Spine e relatório HTML atualizados
- (event) gate concluído com as 3 lentes; artifact republicado
