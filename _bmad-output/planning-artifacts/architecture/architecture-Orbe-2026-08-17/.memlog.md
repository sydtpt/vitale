---
scope: Orbe — arquitetura de conhecimento (docs/ADRs/specs/AGENTS.md) + runtime do monorepo (shared/web/mobile/supabase)
purpose: build-substrate
altitude: initiative
updated: 2026-08-17T21:18
---

- (constraint) Brownfield verificado: packages/shared tem ZERO import de supabase — pureza da camada de dominio ja e convencao de fato.
- (constraint) Cada app cria seu proprio client Supabase: web/src/app/core/supabase/supabase.client.ts e mobile/src/lib/supabase.ts. Nenhum cruzamento web<->mobile.
- (constraint) Shared ja tem estrutura de dominio: fitness/ health/ goals/ week/ period/ chart/ geo/ models/ constants/. Web importa em 90 arquivos, mobile em 100.
- (constraint) DIVERGENCIA ATIVA: 21 basenames duplicados entre web/src e mobile/src. planned-match 77vs110 (43 divergentes), moving-time 54vs80 (60), weekly-volume 89vs86 (57), habit-logic 121vs136 (29), todo-logic 272vs274 (6).
- (constraint) Co-change: 11 de 13 commits que tocaram esses modulos alteraram web E mobile no mesmo commit (85 porcento).
- (constraint) Conhecimento: zero ADRs no repo. Decisoes caras vivem em 24 arquivos de memoria FORA do repositorio.
- (question) Fronteira shared vs app: qual regra decide o que e dominio (shared) e o que e binding de plataforma (app)?
- (event) brownfield sweep concluido; inicio do coaching
- (direction) Usuario: consolidar a arquitetura na melhor forma ANTES de novas features. Nao e divida a administrar — e janela de consolidacao antes do custo crescer.
- (constraint) Usuario nao lembra por que planned-match foi copiado em vez de subir ao shared. Logo: nao houve decisao de plataforma, houve deriva por ausencia de regra. Causa deve ser derivada do codigo, nao da memoria.
- (constraint) moving-time: nucleo IDENTICO (haversine + loop de limiar 0.8 m/s). Unica diferenca real: mobile adiciona adaptador de entrada HealthKit (RoutePoint ISO -> Sample) e refatorou o nucleo para movingTimeFromSamples. Divergencia esta na BORDA DE ENTRADA, nao no calculo.
- (constraint) planned-match: linhas 1-79 substancialmente identicas (so difere estilo de prettier e caminho de import). Unica adicao real do mobile: buildWeek + PlannedDay + rotulos DOW SEG-DOM = APRESENTACAO. Divergencia esta na BORDA DE SAIDA.
- (constraint) mobile/src/lib/planned-match.ts linha 3 carrega comentario 'Espelho de web/... manter as duas em sincronia' — processo manual documentado no codigo ocupando o lugar de uma regra de arquitetura.
- (constraint) TAXONOMIA FRAGMENTADA: GPS_ACTIVITY_IDS definido 2x (web/core/models/activity-types.ts:20 e mobile/lib/workout-types.ts:120, ambos [13,24,37,52]); STRENGTH_IDS e EASY_IDS definidos 2x inline nos planned-match. Enquanto shared/fitness/activity-types.ts detem so os LABELS. Mesmo vocabulario de dominio com tres donos.
- (constraint) Sem CI e sem git hooks (AGENTS.md): qualquer checagem mecanica precisa rodar via npm run test na raiz, que funciona e cobre os workspaces.
- (decision) FRONTEIRA MECANICA adotada: modulo que nao importa API de plataforma (nativo/Angular/React) pertence ao shared. Binds: todo codigo em web/src, mobile/src, packages/shared. Prevents: duplicacao por deriva (5 pares ativos hoje) e divergencia de calculo entre apps. Escolhida sobre a fronteira semantica porque regra que exige julgamento ja falhou 5 vezes neste repo — usuario nao lembra sequer ter deliberado.
- (decision) GUARDA MECANICA: teste rodando em npm run test na raiz falha quando modulo puro existe nos dois apps / basename duplica fora da allowlist. Sem CI e sem hooks hoje, npm run test e o unico ponto de imposicao existente. Usuario recusou criar CI agora.
- (constraint) TERCEIRA CAMADA DE DUPLICACAO: os 10 stores duplicados carregam conhecimento de schema escrito a mao nos dois apps. todos.store web e mobile consultam as mesmas tabelas (todo_templates, todo_occurrences) com queries independentes e ja divergentes (web select(*) sem filtro; mobile select(*).eq(active,true)).
- (question) BURACO NA AD-1: regra mecanica opera por arquivo. Um store importa signals/Zustand, logo fica no app — e leva junto a query pura, preservando a duplicacao. Dois stores podem obedecer a AD-1 ao pe da letra e ainda divergir no schema. Precisa de corolario de granularidade.
- (assumption) @supabase/supabase-js e isomorfico (roda em web e RN), logo NAO e API de plataforma pelo criterio da AD-1. So a construcao do client e especifica (adaptador de storage: localStorage vs AsyncStorage). Modulos de query passariam no teste mecanico e pertenceriam ao shared.
- (decision) ADRs: lote inicial = 24 memorias do Claude Code + varredura de codigo/migrations atras de decisoes documentadas so em comentario. Traz o conhecimento caro para dentro do git.
- (constraint) Medicao: 139 chamadas .from() (54 web + 85 mobile) sobre 19 tabelas. 14 tabelas (74 porcento) sao tocadas pelos DOIS apps — a sobreposicao e a regra, nao a excecao.
- (constraint) packages/shared nao tem bloco dependencies — e pacote zero-dep hoje. Adicionar supabase-js e mudanca real de natureza do pacote.
- (question) DIVERGENCIA ENCONTRADA NA MEDICAO: web consulta from('profiles') em core/auth/profile.service.ts, mobile consulta from('user_profiles') em settings.store. Apenas user_profiles existe nas migrations (20260528120000_user_settings.sql). Ou 'profiles' foi criada fora de migration em producao, ou a leitura do web falha silenciosamente. Verificar antes de tratar como so arquitetura.
- (decision) CONTRATO DE SCHEMA: modulos de query no shared com SupabaseClient injetado como parametro. Cada app segue construindo seu proprio client (storage difere: localStorage vs AsyncStorage). Banco fica como EXCECAO NOMEADA, valida so quando o ganho for reducao de payload ou colapso de round-trip — precedente activity_routes.route_overview. Se a excecao for view, security_invoker=true e obrigatorio. Aceito o custo: shared deixa de ser zero-dep.
- (constraint) ENVELOPE OPERACIONAL: uma unica instancia Supabase (svyyuhxkblufhfvfvqte) serve dev, preview e producao no mobile E o web. Nao ha separacao de ambientes. Mitigacao ja existente: policy de confirmacao explicita antes de supabase db push.
- (constraint) WEB SEM CAMINHO DE DEPLOY: nenhum vercel.json, netlify.toml ou workflow de CI. web/src/environments/environment.prod.ts e placeholder nao preenchido (SUPABASE_PROJECT_URL, REPLACE_WITH_YOUR_DOMAIN) e angular.json NAO tem fileReplacements — arquivo orfao. O web nunca foi publicado.
- (constraint) EDGE FUNCTIONS seguem padrao consistente: so integracao externa que exige segredo server-side (strava-oauth, intervals-link, connections-ingest). Nenhuma logica de negocio propria. Convencao ratificavel.
- (decision) WEB SERA PUBLICADO EM BREVE: invariantes de deploy viram dimensao de primeira classe. Insight: a anon key e publica e protegida por RLS (AGENTS.md diz que environment.ts e versionado de proposito), logo o build do web NAO precisa de injecao de segredo — o padrao de placeholder do environment.prod.ts e desnecessario e enganoso. Deletar o orfao.
- (decision) AMBIENTES: instancia Supabase unica MANTIDA como decisao consciente [ADOPTED], nao omissao. Mitigacao escrita no spine: confirmacao explicita antes de db push, DDL destrutivo nunca sem aval.
- (decision) CONHECIMENTO migra num movimento so: docs/specs/ (55 specs), docs/decisions/ (ADRs), plan.md e tasks.md para _bmad-output/. Tombstone em .claude/specs/README.md resolve as 28 referencias em migrations aplicadas.
- (question) Alvo de hospedagem do web ainda indefinido. Recomendacao: SPA estatica em qualquer host estatico, sem etapa de injecao de segredo. Registrar como Deferred com condicao de revisita.
- (event) Reviewer gate: lint_spine.py 0 achados. Lente 1 (reality-check): security_invoker=true confirmado como recomendacao Supabase para PG15+ via busca web; versoes lidas de package.json; RN 0.81.5 e Expo 54 conferidos contra o npm nesta sessao.
- (decision) Lente adversarial fechou 3 buracos: (1) AD-3 nao forcava descoberta — dois modulos com nomes diferentes para o mesmo conceito passavam; (2) AD-4 nao fixava granularidade — duas queries divergentes da mesma tabela dentro do shared passavam, e a forma de retorno nao tinha dono; (3) AD-7 apontava para um ponto de imposicao que nao executa.
- (constraint) DEFEITO ENCONTRADO: packages/shared tem 3 arquivos de teste (evaluate.test.ts, axis.test.ts, smooth-path.test.ts) e nenhum runner — devDeps so tem typescript, script e echo 'No tests yet'. Nunca rodaram. Originou a AD-13.
- (event) Plano de migracao escrito em _bmad-output/planning-artifacts/plano-de-migracao.md, 8 fases sequenciadas. Plano parcial anterior (plano-mover-specs-para-docs.md) absorvido como Fase 2 e removido para nao competir.
- (event) spine finalized
