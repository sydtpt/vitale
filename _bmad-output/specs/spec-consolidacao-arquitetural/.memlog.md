---
topic: Consolidacao arquitetural do Orbe antes de novas features: eliminar duplicacao entre web e mobile, dar dono unico ao schema e ao vocabulario, trazer decisoes para o repositorio
updated: 2026-08-17T21:23
---

- (direction) Entrada rica: ARCHITECTURE-SPINE.md (13 ADs) + plano-de-migracao.md (8 fases), ambos desta sessao. Extracao direta, sem elicitacao. Spine e plano viram companions ADOTADOS — bmad-architecture os detem, bmad-spec nao edita.
- (capability) CAP-1 Verificacao executavel no nucleo — npm run test roda runner real nos 3 workspaces; resultado dos 3 testes orfaos conhecido.
- (capability) CAP-2 Conhecimento duravel no repositorio — docs/specs com historico preservado; grep .claude/specs so retorna migrations e tombstone.
- (capability) CAP-3 Decisoes registradas — cada decisao hoje so em memoria externa ou comentario vira ADR numerada em docs/decisions.
- (capability) CAP-4 Vocabulario com dono unico — GPS_ACTIVITY_IDS, STRENGTH_IDS, EASY_IDS com definicao unica no nucleo.
- (capability) CAP-5 Logica de dominio sem duplicata — nenhum basename repetido entre web/src e mobile/src fora dos 10 stores.
- (capability) CAP-6 Contrato de schema unico — nenhum .from( fora de packages/shared/src/data/; um modulo dono por tabela.
- (capability) CAP-7 Guarda mecanica ativa — teste falha com violacao introduzida de proposito e passa no estado consolidado.
- (constraint) Migration aplicada nunca e reescrita; as 28 referencias obsoletas ficam, resolvidas por tombstone.
- (constraint) Nenhuma migration em producao nesta consolidacao — o trabalho nao toca o banco.
- (constraint) npm run test na raiz e o unico ponto de imposicao existente: nao ha CI nem git hooks.
- (constraint) Consolidar um par divergente escolhe UMA das versoes — pode mudar comportamento de um dos apps. moving-time e o de maior risco (60 linhas divergentes).
- (assumption) @supabase/supabase-js e isomorfico e nao conta como API de plataforma — base da AD-4 e da CAP-6.
- (assumption) Os 3 testes orfaos do nucleo podem estar quebrados; nunca executaram, entao o resultado e desconhecido, nao presumido verde.
- (question) web consulta from('profiles'), que nao existe em nenhuma migration; mobile consulta from('user_profiles'), que existe. Tabela criada fora do versionamento ou leitura que falha calada?
- (question) Alvo de hospedagem do web, dado que sera publicado em breve.
- (capability) CAP-8 Configuracao que nao mente — environment.prod.ts orfao removido e permissao obsoleta do mkdir removida. Adicionada na Pass 2.
- (event) Self-validate Pass 1 (coerencia, Spec Law 1-6 e 8): aprovado. 8 capabilities com intent+success; 7 constraints que descartam opcoes reais; 6 non-goals; success signal demonstravel.
- (event) Self-validate Pass 2 (preservacao): 1 lacuna encontrada e corrigida — Fase 1 do plano (limpar configuracao morta) nao tinha capability cobrindo; virou CAP-8. Demais fases mapeiam 1:1 nas CAP-1 a CAP-7. Nenhum conteudo load-bearing descartado.
- (event) spec finalizado
